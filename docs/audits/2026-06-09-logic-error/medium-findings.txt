### [MEDIUM] Source hotspots from all branches are matched only against the Cloud main branch (no branch param on the search)
- unit: migrate-issuesync | file: internal/migrate/tasks_hotspotsync.go:159-221, 226-235, 299-305, 456-484 | cat: logic-error
- DESCRIPTION: loadMatchableHotspots filters extract items only by serverURL and project key, NOT by branch — getProjectHotspotsFull is extracted per branch (forEachProjectBranch), so the actionable set contains hotspots from every extracted branch. resolveAndSyncHotspot/findCloudHotspotCandidates build the Cloud /api/hotspots/search request with projectKey + files + organization but never set a `branch` parameter, so the search runs against the Cloud project's MAIN branch only. A feature-branch source hotspot whose file+line has no main-branch counterpart resolves to not_found and its triage state is silently dropped; worse, if a different main-branch hotspot sits on the same line it can be picked and the wrong hotspot gets the triage state.
- WHY: The verified target had 4 long-lived branches, so non-main hotspot triage is never synced. Any reviewed hotspot that exists on a non-main branch (or only on a branch) is lost or mis-applied because the matcher only ever inspects the main branch on Cloud.
- FIX: Carry the source branch through loadMatchableHotspots (parseMatchableHotspot already has the data in the extract metadata) and pass it as the `branch` param to /api/hotspots/search when non-main. Alternatively, restrict the actionable set to main-branch hotspots if main-only sync is the intended scope, and log the skipped branch hotspots so the loss is explicit.
- VERDICT: REAL bug, confirmed by reading the code and the migration's own branch-import path. Three premises all hold: (1) The extract is per-branch — getProjectHotspotsFull runs via forEachProjectBranch (internal/extract/tasks_projectdata.go:111-143) and tags each record with `branch`. (2) loadMatchableHotspots (tasks_hotspotsync.go:456-484) filters ONLY by serverURL + project key, never by branch, so the actionable set legitimately contains hotspots from every extracted branch. (3) findCloudHotspotCandidates (lines 299-324) builds api/hotspots/search with projectKey+files+organization and NO branch pa

### [MEDIUM] Source issues from all branches are matched only against the Cloud main branch (no branch param on the search)
- unit: migrate-issuesync | file: internal/migrate/tasks_issuesync.go:459-526, 552-580, 612-634, 848-908 | cat: logic-error
- DESCRIPTION: loadMatchableIssues filters extract items only by serverURL + projectKey; getProjectIssuesFull is extracted per branch, so the actionable set spans every branch (countDistinctBranches at line 497 even reports the count). findCloudIssueCandidates sets componentKeys (cloudKey:filePath) + organization + rules + issueStatuses but never a `branch` parameter, so /api/issues/search resolves against the Cloud main branch only. Feature-branch source issues that have manual triage are searched against main: a missing line yields not_found (triage lost) and a line collision with a different main-branch issue of the same rule yields a single match that gets the feature-branch issue's triage state applied to the wrong (main) issue.
- WHY: Migrations with long-lived branches (verified: 4 branches) silently lose or mis-apply triage state for any manually-triaged issue that lives on a non-main branch, because the matcher never queries the branch the source issue actually came from.
- FIX: Thread the source issue's Branch (already on matchableIssue) into findCloudIssueCandidates and set the `branch` param on /api/issues/search for non-main branches; or explicitly restrict the actionable set to the main branch and log the dropped branch issues so the omission is visible rather than silent.
- VERDICT: REAL bug, confirmed by reading code + SDK + Sonar API semantics + this repo's own transfer-verification doc.

Mechanics verified end-to-end:
1. Source set spans ALL branches. getProjectIssuesFull is extracted per-branch via forEachProjectBranch (internal/extract/tasks_projectdata.go:70-74 sets a per-branch `branch` param). loadMatchableIssues (tasks_issuesync.go:856-897) filters extract items ONLY by serverURL (857) and projectKey (860) — there is NO branch filter — and records each issue's source Branch at line 895. So `sourceIssues`/`actionable` genuinely contain issues from every branch; co

### [MEDIUM] Portfolio project/org indexes are keyed by bare portfolioKey, dropping the server scope and cross-merging data across servers
- unit: migrate-portfolios | file: internal/migrate/tasks_portfolios.go:213-260, 106, 138 | cat: logic-error
- DESCRIPTION: buildPortfolioProjectList (line 231: out[portfolioKey]) and buildPortfolioOrgIndex (line 256: out[portfolioKey]) build their output maps keyed ONLY by the bare source portfolio key, with no server-URL prefix. runConfigurePortfolios then looks them up by the same bare sourceKey at line 106 (orgs := portfolioOrgs[sourceKey]) and line 138 (cloudKeys := portfolioProjects[sourceKey]). Every other portfolio map in the run is server-scoped: createProjects/buildProjectCloudKeyIndex uses serverURL+key (line 202), indexPortfolioCompositions uses serverURL+"|"+key (line 445), structure.MapPortfolios uses item.ServerURL+portfolioKey (portfolios.go line 32), and the report side keys by serverURL+"|"+sourceKey (portfolio_hierarchy.go line 308).
- WHY: ExtractMapping is map[serverURL]extractID and ReadExtractData (extract.go line 82-95) iterates over ALL servers, so a `migrate` run can span multiple SonarQube Server instances. If two source servers each have a portfolio with the same key (a common case: default/seeded portfolio keys, or two instances of the same org), the project list and org list for that key get merged across both servers. A MANUAL portfolio on server A would then receive cloud project refs resolved from server B's projects (resolveProjectBranchRefs would PATCH the wrong projects into the portfolio), and the org-prefix set used to rewrite a REGEXP would include the other server's orgs. The per-project resolution itself is correct (line 220 uses item.ServerURL+refKey), but the correct per-server results are then collapsed into a server-agnostic key, corrupting the configuration of portfolios that share a key across servers.
- FIX: Key both maps by item.ServerURL+"|"+portfolioKey (matching indexPortfolioCompositions), and in runConfigurePortfolios look them up with serverURL+"|"+sourceKey instead of bare sourceKey. The serverURL is already extracted at line 100.
- VERDICT: REAL bug. Concrete path: (1) Multi-server runs are supported — migrate.go:145 calls GetUniqueExtracts over the whole export dir with no single-server filter, so e.Mapping holds all server URLs; ReadExtractData (structure/extract.go:82-95) iterates every server and tags each ExtractItem with ServerURL. (2) buildPortfolioProjectList (tasks_portfolios.go:231 out[portfolioKey]) and buildPortfolioOrgIndex (line 256 out[portfolioKey]) key their output by the BARE portfolioKey, dropping item.ServerURL, even though the per-project lookup at lines 220/244 correctly uses item.ServerURL+refKey. runConfig

### [MEDIUM] Pre-existing gate conditions may not be cleared if SQC returns string condition IDs
- unit: migrate-configure | file: internal/migrate/tasks_configure.go:322-333 | cat: api-misuse
- DESCRIPTION: clearTargetGateConditions iterates gate.Conditions (from /api/qualitygates/show) and skips any condition whose cond.ID == 0, then calls DeleteCondition with cond.ID (an int). QualityGateCondition.ID is typed as `int` with json:"id" in lib/sq-api-go/types/qualitygates.go. If SonarQube Cloud serializes the condition id as a JSON string (recent SQ/SQC versions have moved several id fields to non-numeric/string identifiers), the int unmarshal silently fails and every condition ends up with ID==0.
- WHY: When wasPreexisting is true, addGateConditions calls clearTargetGateConditions to wipe the target gate's existing conditions so the migrated set is authoritative (never a union). If condition IDs decode to 0, the `if cond.ID == 0 { continue }` guard skips every delete, the clear is a silent no-op (it even logs 'cleared pre-existing conditions ... count=N' with the full count), and the subsequent CreateCondition POSTs then ADD the migrated conditions on top of the pre-existing ones — producing exactly the source+target union the override logic was written to prevent, or duplicate/conflicting conditions on the same metric.
- FIX: Confirm the wire type of the qualitygates/show condition `id` against the target SonarCloud version. If it can be a string, change QualityGateCondition.ID to a json.Number/string (or add a tolerant UnmarshalJSON), and treat an unparseable/empty id as an error (log Warn) rather than silently skipping via the ID==0 sentinel, so a failed clear is visible instead of silently degrading to union behavior.
- VERDICT: REAL bug, though the claim's stated internal mechanism is partly wrong; the consequence is correct (arguably worse). QualityGateCondition.ID is a plain `int` with json:"id" (lib/sq-api-go/types/qualitygates.go:11), no custom UnmarshalJSON. SonarQube/SonarCloud migrated quality-gate condition IDs from integers to string-style IDs (SONAR-13248/13249/13300; integers removed in SQ 10.0), and the repo's own production extract data (go/migration-files/*/getGateConditions/results.*.jsonl) shows live condition IDs as UUID strings like "dc99dd69-7c89-4d57-be12-a8e270044031" — so a string id from /api/q

### [MEDIUM] Global --concurrency limit is never enforced globally; e.Sem channel is allocated but used only for its capacity, so concurrency multiplies by parallel tasks per phase
- unit: migrate-planner | file: internal/migrate/migrate.go:252 (allocation); consumed via cap(e.Sem) in internal/migrate/helpers.go:120,209,406 and tasks_associate.go:478, tasks_projectdata.go:55, tasks_setglobalsettings.go:528 | cat: concurrency
- DESCRIPTION: RunMigrate creates the shared semaphore `Sem: make(chan struct{}, cfg.Concurrency)` and stores it on the single Executor that is shared by every task. But no code ever sends to or receives from `e.Sem` — every consumer only reads `cap(e.Sem)` and feeds it into a brand-new per-task `errgroup` via `g.SetLimit(cap(e.Sem))` (helpers.go forEachMigrateItemImpl/forEachExtractItem/the bounded helper at ~406, and several task files). Because runPhase launches all tasks in a phase concurrently (one g.Go per task in the errgroup at migrate.go:278), and each of those tasks independently creates its own errgroup capped at cap(e.Sem), the actual number of in-flight API workers is up to cap(e.Sem) x (number of tasks running in parallel in that phase), not cfg.Concurrency.
- WHY: The operator sets --concurrency (default 25) expecting at most 25 concurrent Cloud API calls. With a topologically-phased plan of ~57 task defs, phases routinely contain multiple parallel tasks (e.g. setProjectProfiles, setProjectGates, setProjectSettings, setProjectTags, setProjectLinks all share the same dependency level). If 5 such tasks run in parallel, real in-flight concurrency reaches 5x25 = 125 simultaneous requests against SonarCloud. This directly under-throttles relative to the configured budget, driving more 429 rate-limit responses (the very thing ratelimit.go tracks) and heavier load than the operator asked for. The channel-as-semaphore was clearly the intended global gate (it is a buffered chan struct{} sized to Concurrency and shared on the one Executor) but the gate is never acquired/released.
- FIX: Either (a) actually gate on the shared channel inside each worker — `e.Sem <- struct{}{}` before fn and `<-e.Sem` after (deferred), in addition to or instead of the per-task SetLimit — so the cap is global across all concurrently-running tasks; or (b) if a true global limiter is wanted, replace the chan with a shared `golang.org/x/sync/semaphore.Weighted` acquired per item. If per-task limiting is genuinely intended, remove the misleading shared `Sem` channel and document that --concurrency is per-task, because the current code neither enforces a global cap nor makes the per-task semantics explicit.
- VERDICT: REAL bug, confirmed end-to-end. (1) migrate.go:252 allocates Sem=make(chan struct{}, cfg.Concurrency) on the single shared Executor. (2) A repo-wide grep proves e.Sem is NEVER acquired/released in internal/migrate/ — every reference is cap(e.Sem) (helpers.go:120,209,406; tasks_associate.go:478; tasks_projectdata.go:55; tasks_setglobalsettings.go:528). Real semaphore acquire/release (acquireSem + <-e.Sem) exists only in internal/extract/, confirming the channel-as-global-gate was intended but never wired up in migrate. (3) runPhase (migrate.go:272-306) has NO g.SetLimit, so its errgroup launche

### [MEDIUM] Shared *FixSuggestionsAiCodeFix mutated across the per-org loop corrupts EnabledProjectKeys for some orgs
- unit: migrate-misc | file: internal/migrate/ai_codefix.go:382-392 | cat: data-loss
- DESCRIPTION: applyAiCodeFixDecisions iterates orgList and, per org, does `payload := *primary.PatchPayload`. This is a SHALLOW copy: `payload.AiCodeFix` still points at the SAME *types.FixSuggestionsAiCodeFix struct that `primary.PatchPayload.AiCodeFix` references (confirmed: FixSuggestionsOrgConfig.AiCodeFix is a *FixSuggestionsAiCodeFix in lib/sq-api-go/types/fix_suggestions.go). Line 391 then assigns `payload.AiCodeFix.EnabledProjectKeys = mapped`, writing through the shared pointer. The next org reads the GUARD `len(payload.AiCodeFix.EnabledProjectKeys) > 0` (line 387) against the previous org's result instead of the original list. The re-mapping input is `primary.SourceProjectKeys` (stable), but if an earlier org maps to an EMPTY list (all its projects dropped), the shared field becomes empty and the guard is false for every later org, so later orgs are never re-mapped and get PATCHed with the wrong (empty) project list.
- WHY: Enablement ENABLED_FOR_SOME_PROJECTS with two target orgs A and B, source projects [P1,P2]. orgList is sorted; say B sorts first and all its candidate projects drop (they belong to org A) so it maps to []. Iteration 1 (B): guard true (initial list non-empty), maps to [] and writes shared EnabledProjectKeys=[]. Iteration 2 (A): guard now reads shared []=len 0 -> false, block skipped, so A is PATCHed with enablement=ENABLED_FOR_SOME_PROJECTS and an EMPTY project list. Org A's projects P1/P2 are silently never enabled — a correctness/data-loss bug whose outcome depends on org sort order.
- FIX: Deep-copy the AiCodeFix struct per iteration before mutating, e.g. `payload := *primary.PatchPayload; if payload.AiCodeFix != nil { ac := *payload.AiCodeFix; payload.AiCodeFix = &ac }`, and base the re-map decision on `len(primary.SourceProjectKeys) > 0` (or on `primary.PatchPayload.AiCodeFix.EnabledProjectKeys`) rather than on the per-iteration `payload` that has already been overwritten.
- VERDICT: CONFIRMED REAL. I could not refute the claim; the execution path holds.

Mechanism (all verified against source):
1. lib/sq-api-go/types/fix_suggestions.go:13 — FixSuggestionsOrgConfig.AiCodeFix is a POINTER (*FixSuggestionsAiCodeFix). So `payload := *primary.PatchPayload` (ai_codefix.go:383) is a shallow copy: payload.AiCodeFix aliases primary.PatchPayload.AiCodeFix.
2. `primary` is captured ONCE before the loop (line 365) via pickPrimaryDecision, so its PatchPayload.AiCodeFix pointer is shared across every org iteration.
3. Line 391 `payload.AiCodeFix.EnabledProjectKeys = mapped` writes THRO

### [MEDIUM] Unified (shape 4) config drops top-level debug / skip_profiles so debug logging is silently disabled
- unit: migrate-misc | file: internal/migrate/config_file.go:146-178 | cat: incorrect-condition
- DESCRIPTION: In the unified (Source/Target) branch of toMigrateConfig, the only top-level fields propagated are Concurrency (as fallback) and ExportDirectory; Debug, SkipProfiles and IncludeProjectData are never copied from the top-level configFileShape. The default (flat) branch and the recursive Migrate branch DO copy Debug/SkipProfiles, creating a shape-dependent inconsistency. cmd/transfer.go reads migrateCfg.Debug (line 272) and only the migrate command can re-supply debug via a CLI flag.
- WHY: A user with a unified config file that sets top-level `"debug": true` and runs `transfer --config file` gets cfg.debug=false because toMigrateConfig (shape 4) never copies s.Debug. Debug logging the user explicitly requested in the config is silently ignored. Same for skip_profiles in the unified shape.
- FIX: In the unified branch, after pulling Target fields, also set cfg.Debug = s.Debug and cfg.SkipProfiles = s.SkipProfiles (and IncludeProjectData if intended), mirroring the flat branch — or factor a shared 'apply top-level defaults' helper used by all four shapes so the precedence is consistent.
- VERDICT: REAL bug, confirmed with a concrete reachable execution path. In internal/migrate/config_file.go the unified (shape 4) branch of toMigrateConfig (lines 148-178) propagates only Concurrency (as fallback), ExportDirectory, SkipIssueSync, SkipProjectDataMigration, and target fields. It never copies the top-level s.Debug or s.SkipProfiles (nor IncludeProjectData). The flat/default branch (lines 209-211) and the recursive Migrate branch DO copy Debug and SkipProfiles, so the inconsistency is real and shape-dependent. The side-sectioned (sonarCloud) branch also drops them, but the claim's focus on t

### [MEDIUM] Partial per-project task dir created before data is written makes resume silently skip an incomplete task
- unit: extract-core | file: internal/extract/helpers.go:59-94 (with internal/extract/extract.go:380-394 and internal/common/store.go:115-120) | cat: data-loss
- DESCRIPTION: forEachDepFiltered creates the task output directory via e.Store.Writer(taskName) at line 80 — which calls NewChunkWriter -> os.MkdirAll — BEFORE the per-item fetch/write loop runs (lines 86-93). The directory therefore exists on disk as soon as the task starts, regardless of how many of the per-project records were actually fetched and written. Resume logic in extract.go's filterCompleted (lines 380-394) skips any task whose directory already exists, using DataStore.TaskDirExists (store.go:117-120), which only does an os.Stat on the dir and checks IsDir() — it does NOT consult any completion sentinel or record count.
- WHY: Run extract with a fixed --extract_id (or re-run the same generated ID to resume). A per-project task such as getProjectSettings / getProjectMeasures / getProjectGroupsPermissions begins, mkdir's its task dir, writes records for the first N projects, then hits a fatal (non-403/404) error on project N+1 (e.g. a 500, a network drop, or context cancellation). g.Wait() returns the error, executePhases aborts, MarkComplete is never reached for that task. On the next resume with the same extractID, filterCompleted sees the existing (partial) task dir and removes the task from the plan entirely. The remaining projects' settings/measures/permissions are never fetched, and the downstream structure/migrate phases consume a silently truncated dataset — wrong data with no error surfaced.
- FIX: Gate resume on an explicit per-task completion sentinel rather than mere directory existence. E.g. write a `.done` marker file inside the task dir from executePhases after store.MarkComplete (only on success), and have TaskDirExists / filterCompleted require that marker. Alternatively, write task output to a temp dir and atomically rename to the final dir name only after the task's errgroup returns nil. Defer the Writer/mkdir creation until the first WriteChunk so failed tasks leave no dir at all (still insufficient for partial multi-project writes, so the sentinel approach is preferred).
- VERDICT: I attempted to refute this claim but could not — it describes a concrete, reachable silent-data-loss path. Verified mechanics:

1. helpers.go:80 — forEachDepFiltered calls e.Store.Writer(taskName) BEFORE the per-item fetch loop (lines 86-93). Writer → NewChunkWriter → os.MkdirAll (writer.go:26), so the task dir exists on disk the instant the task starts, independent of how many records get written.

2. The resume gate is purely disk-based. filterCompleted (extract.go:380-393) drops any task where TaskDirExists is true. TaskDirExists (store.go:117-120) is a bare os.Stat + IsDir() — no completio

### [MEDIUM] Portfolio dedup by project-composition hash silently drops distinct portfolios and is map-iteration-order dependent
- unit: structure-core | file: internal/structure/portfolios.go:65-88 | cat: data-loss
- DESCRIPTION: MapPortfolios deduplicates portfolios using generateHashID(projectList), where projectList is the SET of project refKeys belonging to the portfolio. Two genuinely different portfolios that happen to contain the same set of projects (or two empty/REGEXP/TAG portfolios that both resolved to zero projects in getPortfolioProjects) produce the SAME hashID. uniquePortfolios[hashID] = portfolioDetails[key] then overwrites: only ONE of the colliding portfolios survives into portfolios.csv, and which one survives depends on Go's nondeterministic map iteration order over portfolioProjects.
- WHY: Scenario: a SonarQube Server instance has portfolio 'Frontend' and portfolio 'Frontend (release)', both manually composed of exactly projects {app-web, app-mobile}. generateHashID hashes the sorted project list, yielding identical IDs. The second iteration overwrites the first in uniquePortfolios, so only one Portfolio row reaches portfolios.csv with one SourcePortfolioKey. Downstream migrate code (internal/migrate/tasks_portfolios.go lines 99, 113, 413; tasks_create.go line 368) keys every operation off source_portfolio_key, so the dropped portfolio is never created on SonarQube Cloud — a silent migration data loss. The same collision is near-certain for any two empty/regex/tag portfolios whose getPortfolioProjects output is empty (both hash the empty list). Because the survivor is chosen by map iteration order, runs are also non-reproducible.
- FIX: Do not deduplicate portfolios by project composition. Key uniquePortfolios by the natural unique key (item.ServerURL + portfolioKey, i.e. the same 'key' already used in portfolioProjects/portfolioDetails) so every distinct source portfolio yields its own row. If dedup of truly identical portfolios is intended, include SourcePortfolioKey/Name in the hash input, or drop the hash entirely and emit one row per (serverURL, portfolioKey).
- VERDICT: REFUTATION ATTEMPT FAILED — the core mechanism is real, though one of the two scenarios the reviewer cites is a false positive.

VERIFIED MECHANICS (portfolios.go:65-88): The dedup map is keyed solely by generateHashID(projectList), where projectList is the SET of resolved refKeys (lines 68-73). generateHashID (174-183) sorts and SHA-256-hashes ONLY that list — it is NOT salted with portfolio key, name, selectionMode, regexp, or tags. `uniquePortfolios[hashID] = portfolioDetails[key]` overwrites on collision, and the loop iterates `portfolioProjects` (a Go map), so the survivor is nondetermini

### [MEDIUM] getNewCodeDef fallback returns days/30 instead of the documented zero value for unmapped NCD types (REFERENCE_BRANCH/SPECIFIC_ANALYSIS)
- unit: structure-core | file: internal/structure/projects.go:245-254 | cat: incorrect-condition
- DESCRIPTION: The newCodeMappings doc comment (lines 17-24) states that projects whose main-branch NCD uses REFERENCE_BRANCH or SPECIFIC_ANALYSIS (which do not exist on SonarQube Cloud, issue #135) must 'fall back to the SQC org default by returning a zero newCodeDefinition from getNewCodeDef.' MapNewCodeDefinitions skips those types (continue at line 94), so they are never stored in the defs map. But when getNewCodeDef finds no entry it returns newCodeDefinition{Type:"days", Value:30}, NOT a zero value. So those projects are forced to a NUMBER_OF_DAYS=30 new-code period on Cloud rather than inheriting the org default.
- WHY: A project whose main branch uses REFERENCE_BRANCH new-code definition on the Server: MapNewCodeDefinitions skips it, getNewCodeDef returns {days,30}, and projects.csv gets new_code_definition_type=days, new_code_definition_value=30. The migrate step then sets an explicit 30-day NCD on the new Cloud project instead of leaving it on the org default, contradicting the stated #135 behavior and silently changing the project's quality-gate new-code baseline. The same wrong default also hits any project that simply has no getNewCodePeriods record at all.
- FIX: Return the zero value (newCodeDefinition{}) on the not-found path so the migrate side can detect 'unset' and skip the newCodeDefinition params, letting the org default apply. If a global tool default of days/30 is genuinely desired for projects that DID have a mapped NCD but with a missing value, apply that default at the point the value is read (MapNewCodeDefinitions line 98-100), not in the not-found fallback.
- VERDICT: I tried to refute this and could only partially. The reviewer's PRIMARY example (REFERENCE_BRANCH/SPECIFIC_ANALYSIS) is mostly wrong, but their PARENTHETICAL observation ("the same wrong default also hits any project that has no getNewCodePeriods record at all") is a genuine, reachable, uncorrected bug. Net: the finding is REAL.

MECHANISM CONFIRMED:
- getNewCodeDef (projects.go:245-254) returns {Type:"days",Value:30} on any miss. The doc comment (lines 17-24) claiming it returns a "zero newCodeDefinition" is factually wrong about its own code.
- That days/30 flows into projects.csv (NewCodeDe

### [MEDIUM] coerceCSVValue turns numeric-looking strings (e.g. all-digit project keys) into float64, breaking downstream string type assertions and dropping projects
- unit: structure-core | file: internal/structure/csv.go:172-195 | cat: logic-error
- DESCRIPTION: LoadCSV runs every cell through coerceCSVValue, which (lines 187-191) parses any value that round-trips as a number into a float64. A purely numeric project key, org key, or value column thus comes back as float64 instead of string. RunMappings (structure.go lines 58-64) reads these via type assertions: serverURL,_ := p["server_url"].(string); key,_ := p["key"].(string); orgKey,_ := p["sonarqube_org_key"].(string). If any of those columns was numeric, the assertion fails, the variable is "", and the row is excluded from projectOrgMapping (the `if serverURL!="" && key!="" && orgKey!=""` guard at line 62 drops it).
- WHY: SonarQube project keys are arbitrary strings and are frequently all-digits (e.g. a key like "1234567" from an ALM repo id, or numeric internal keys). After structure writes projects.csv, RunMappings reloads it; coerceCSVValue converts key "1234567" to float64(1234567). The `.(string)` assertion fails, key becomes "", and that project is omitted from projectOrgMapping, so its templates/profiles/gates/groups org resolution silently loses that project. Numeric keys also round-trip incorrectly for large values (float64 precision / scientific notation).
- FIX: Do not blanket-coerce numbers in CSV cells. Restrict coercion to columns/struct fields that are actually typed (booleans, JSON arrays/objects, and the specific `any` value columns), or keep all cells as strings in LoadCSV and let callers coerce explicitly. At minimum, exclude key-like identifier columns from numeric coercion, and make the RunMappings reads tolerant (fmt.Sprintf or a getString-style helper) instead of bare string assertions.
- VERDICT: CONFIRMED REAL via end-to-end reproduction with the actual repo code (ExportCSV -> LoadCSV -> the exact RunMappings map-build loop). Causal chain verified:

1. Project.Key (internal/structure/projects.go:211) is set to the RAW SonarQube project key from the extract JSON (getString(detail,"key")), which is an arbitrary string and CAN be purely numeric (e.g. an ALM repo-id used as the project key, or a numeric internal key).
2. ExportCSV writes it as a plain string cell "1234567" in projects.csv (Key is a reflect.String, hits the default branch of serializeCSVValue).
3. LoadCSV (csv.go:92) runs 

### [MEDIUM] addDefaultGates assigns every server's default gate to every org across all servers (cross-server contamination)
- unit: structure-entities | file: internal/structure/gates.go:82-99 | cat: logic-error
- DESCRIPTION: addDefaultGates iterates over the full set of orgKeys (uniqueOrgKeys spans ALL servers in the export) and the full gates map (entries from ALL servers, keyed serverURL+name) and writes results[orgKey + gate.Name] for every default gate, never checking that gate.ServerURL corresponds to the server that orgKey belongs to.
- WHY: When an export directory contains extracts from two (or more) SonarQube Servers (ReadExtractData explicitly iterates the multi-entry ExtractMapping and MapGates is built around serverURL-prefixed keys, so multi-server is a supported input), the default quality gate of server A gets emitted as a Gate row for every org derived from server B, with IsDefault=true. The migrate phase then creates/assigns server A's gate inside server B's SonarCloud org. The single-server transfer path is unaffected, which is why existing tests (all single-server) pass.
- FIX: Scope the inner loop to the org's server: pass/derive the server for each orgKey and skip gates whose gate.ServerURL != that server, e.g. only add default gates whose ServerURL matches the binding/server that produced orgKey. (Same fix pattern needed in addDefaultProfiles and addDefaultGroups.)
- VERDICT: REAL bug — I could not refute it; the claimed execution path is concrete and reachable.

Evidence (all in go/internal/structure):

1. Multi-server input is genuinely supported. ReadExtractData (extract.go:82-95) iterates the full ExtractMapping (serverURL -> extractID built by GetUniqueExtracts/buildURLMappings, one entry per distinct extract.json "url") and tags every record with its ServerURL. So the `gates` map in MapGates (gates.go:17-30, keyed serverURL+name) legitimately holds default-gate entries from multiple servers at once.

2. uniqueOrgKeys (profiles.go:147-157) returns ALL distinct

### [MEDIUM] addDefaultProfiles assigns every server's default profiles to every org across all servers
- unit: structure-entities | file: internal/structure/profiles.go:79-89 | cat: logic-error
- DESCRIPTION: addDefaultProfiles loops orgKeys (all servers) x profileMap (all servers) and calls addProfile(orgKey, profile.ServerURL, profile.Key) for every IsDefault profile with no check that profile.ServerURL matches the server orgKey came from.
- WHY: In a multi-server export, server A's default profiles (and via the addProfile parent recursion, their parent chains) are attached to server B's orgs. Those profiles are then created/bound in the wrong SonarCloud org during migrate, polluting orgs with languages/profiles that never existed on their source server. Only manifests with 2+ servers in one export dir; single-server runs are correct.
- FIX: Restrict the default-profile loop to profiles whose ServerURL matches the server that orgKey belongs to (mirror the server-scoped lookups already used in mapProjectProfiles / addTemplateGroups).
- VERDICT: CONFIRMED REAL. addDefaultProfiles (internal/structure/profiles.go:79-89) loops uniqueOrgKeys(projectOrgMapping) — the union of org keys across ALL servers in the export — crossed with profileMap — the union of profiles across ALL servers — and for every IsDefault profile calls addProfile(profileMap, results, orgKey, profile.ServerURL, profile.Key) with NO guard that profile.ServerURL is the server orgKey came from. The correct sibling mapProjectProfiles (lines 60-76) resolves orgKey := projectOrgMapping[item.ServerURL+projectKey], pairing each project's profiles only with its own server's org

### [MEDIUM] applyReportPackaged keys the branch row by sourceBranch while CE-task/pre-created events key by targetBranch, splitting one branch into two rows
- unit: summary-collect | file: internal/report/summary/collect_runtime.go:274-293 | cat: logic-error
- DESCRIPTION: applyReportPackaged builds/looks up its BranchStat with key = firstNonEmpty(evStr(a,"sourceBranch"), evStr(a,"targetBranch"), evStr(a,"project")), i.e. it prefers the SOURCE branch name. But applyTaskSubmitted keys by evStr(a,"targetBranch") and applyAnalysisPreCreated keys by evStr(a,"branch") which the emitter sets to targetBranch. When the source branch name differs from the cloud target branch name, the three events that describe the SAME migrated branch land in two different map entries: one (keyed by source name) carrying Issues/ExternalIssues/Components/ActiveRules/ZipBytes and Status="packaged" but no TaskID, and another (keyed by target name) carrying TaskID and Status="submitted"/Type.
- WHY: For the main branch the migrate engine remaps the source main name to the cloud main name: importAndRecordBranch sets targetBranch = bctx.SCMainBranch when branch.IsMain && SCMainBranch != "" (tasks_projectdata.go:207-210), and importBranch logs 'report packaged' with sourceBranch=input.Branch and targetBranch=targetBranch (tasks_projectdata.go:461-462) while 'CE task submitted' logs only targetBranch (tasks_projectdata.go:292) and 'analysis pre-created' logs branch=targetBranch (tasks_projectdata.go:427-428). This is the documented common case (source main 'develop'/'master' -> cloud main 'main'). Result: the runtime Branches table renders two rows for one logical branch, the packaged row shows 0 task submission and the submitted row shows 0 issues/components, and the throughput totals computed in collectRunEvents (collect_runtime.go:343-357) increment BOTH BranchesPackaged (for the packaged-only row) AND TasksSubmitted (for the submitted-only row) for that single branch, inflating the per-branch counts and corrupting the per-branch detail table.
- FIX: Make all three handlers agree on a single key. Since CE-task-submitted and analysis-pre-created can only emit targetBranch, change applyReportPackaged to prefer targetBranch: key := firstNonEmpty(evStr(a, "targetBranch"), evStr(a, "sourceBranch"), evStr(a, "project")). (Alternatively emit sourceBranch on the CE-task/pre-created events too, but targetBranch is the only field common to all three.)
- VERDICT: REAL bug, but mis-rated as high; correct severity is medium (report-only artifact, not migration corruption).

Code mechanics confirmed exactly as claimed:
- applyReportPackaged (collect_runtime.go:276) keys by firstNonEmpty(sourceBranch, targetBranch, project) -> prefers SOURCE name.
- applyTaskSubmitted (line 288) keys by targetBranch; applyAnalysisPreCreated (line 297) keys by "branch" which the emitter sets to targetBranch (tasks_projectdata.go:428).
- Emitters verified: "report packaged" logs sourceBranch=input.Branch AND targetBranch (tasks_projectdata.go:461-462); "CE task submitted" lo

### [MEDIUM] ProcessScanDetails requires $.projectKey, but the extractor never writes projectKey into analysis items — every scan is dropped
- unit: report-common-a | file: internal/report/common/pipelines.go:20-40 | cat: logic-error
- DESCRIPTION: ProcessScanDetails reads getProjectAnalyses items and extracts projectKey via report.ExtractString(item.Data, "$.projectKey") (line 24). Line 25 skips any item where projectKey == "". The CI pipeline overview and per-project scan detail tables are built exclusively from these scans. However, the getProjectAnalyses extractor (internal/extract/tasks_misc.go:36-41 -> forEachProjectCE, tasks_misc.go:103) enriches each analysis row only with {"serverUrl": e.ServerURL} via EnrichAll (internal/common/helpers.go). It does NOT add a projectKey field. The raw SonarQube api/project_analyses/search 'analyses' array elements contain fields like key, date, events, detectedCI, projectVersion — the project key is only the request parameter and is not present in each result row. As a result item.Data has no 'projectKey' key, ExtractString returns "", and the projectKey == "" guard skips 100% of analyses.
- WHY: In a real run, ProcessScanDetails always returns an empty ProjectScans map. GeneratePipelineMarkdown therefore emits an empty 'CI Environment Overview' and empty 'Project Scan Details' section, and GenerateProjectMetricsMarkdown's 'Most Recent Scan' column (which calls findMostRecentScan over these scans) is always blank. The unit test TestProcessScanDetails passes only because its fixtures manually inject "projectKey": testProj1 into each analysis entry (common_test.go:293-294) — data the actual extractor never produces. Compare to other per-project extractors (e.g. tasks_projects.go:172 projectMeasuresTask, tasks_projectdata.go) which explicitly enrich with map[string]any{"projectKey": key, ...}; the analyses path does not.
- FIX: Enrich getProjectAnalyses (and getProjectTasks) items with projectKey in forEachProjectCE — pass the per-project key into the metadata map (e.g. map[string]any{"serverUrl": e.ServerURL, "projectKey": key}) when calling enrichAll in tasks_misc.go. Alternatively, change ProcessScanDetails to read the project key from the field the analyses API actually provides. Make the test fixture reflect what the extractor really writes (drop the manual projectKey injection) so the regression is caught.
- VERDICT: REAL bug, could not refute. Confirmed end-to-end against real production data.

ROOT CAUSE: internal/extract/tasks_misc.go:124 — forEachProjectCE enriches each getProjectAnalyses row with only map[string]any{"serverUrl": e.ServerURL}. The project key (`key`, line 107) is in scope but never added to the enrichment metadata. Every sibling per-project extractor does it correctly (tasks_projects.go:172, helpers.go:242/289, tasks_issues.go:72 all use {"projectKey": key, "serverUrl": ...}); the analyses path is the lone omission.

DATA PROOF: Scanned every getProjectAnalyses/results*.jsonl under mig

### [MEDIUM] Active Gates detail table reads per-condition metric keys that the gate processor never populates
- unit: report-maturity-a | file: internal/report/maturity/gates.go:42-72 | cat: data-loss
- DESCRIPTION: collectGateDetail copies gate["new_violations"], gate["new_security_hotspots_reviewed"], gate["new_coverage"], gate["new_duplicated_lines_density"], and gate["other"] into each detail row, and GenerateGateMaturityMarkdown declares matching columns for them. However, common.ProcessQualityGates (internal/report/common/gates.go:13-42) only ever sets the keys server_id, name, is_built_in, is_default, is_cayc, project_count, and a raw conditions slice ([]any{}). It never parses the gate's conditions into the named metric keys the maturity report expects. No code anywhere in internal/report populates new_violations / new_security_hotspots_reviewed / new_coverage / new_duplicated_lines_density / other (grep confirms these strings appear only in maturity/gates.go).
- WHY: In every maturity report run, the "Active Gates" detail table renders the New Violations, Hotspots Reviewed, Coverage, Duplicated Lines, and Other columns as empty cells (FormatValue(nil) returns ""), because the underlying map keys are nil. The report silently omits the actual quality-gate condition thresholds it claims to display, giving a misleading impression that the gates have no conditions. The raw condition data is present in gate["conditions"] but is never decoded into these per-metric fields.
- FIX: In ProcessQualityGates (or a maturity-specific step), parse each gate's conditions array (metric + op + error threshold) and populate new_violations, new_security_hotspots_reviewed, new_coverage, new_duplicated_lines_density, and bucket the remaining conditions into other, before collectGateDetail reads them. Alternatively, drop the columns if the data is genuinely unavailable rather than emitting empty columns.
- VERDICT: REAL bug, confirmed by reading the full execution path. internal/report/maturity/gates.go GenerateGateMaturityMarkdown declares detail columns for New Violations (key new_violations), Hotspots Reviewed (new_security_hotspots_reviewed), Coverage (new_coverage), Duplicated Lines (new_duplicated_lines_density), and Other (other). collectGateDetail (gates.go:62-72) reads exactly these keys from the per-gate map. But the only function feeding that map, common.ProcessQualityGates (internal/report/common/gates.go:13-42 + ensureGateEntry:44-54), sets only server_id, name, is_built_in, is_default, is_c

### [MEDIUM] countScansByTier reads non-existent project field "30_day_scans", so Usage "Scans/Day" is always 0
- unit: report-maturity-b | file: internal/report/maturity/usage.go:103-113 | cat: logic-error
- DESCRIPTION: buildUsageRows computes the per-tier "Scans/Day" column from tierScans := countScansByTier(projects). countScansByTier reads s30, _ := project["30_day_scans"].(int) off the project map. But project maps are built in common/projects.go (ProcessProjectDetails / processProjectUsage), which only ever set keys server_id, name, key, main_branch, profiles, languages, quality_gate, binding, loc, tier, rules, template_rules, plugin_rules. The key "30_day_scans" is never written onto any project map anywhere in production code (confirmed by repo-wide grep: usage.go:108 is the ONLY reference, and there is no assignment to it). The actual 30-day scan counts live on the scan maps as scan_count_30_days (set in common/pipelines.go). Therefore the type assertion always yields 0, tierScans is all-zero, scansPerDay stays 0, and the "Scans/Day" column in the maturity report's Usage section is always 0.00 for every tier.
- WHY: On any real export, every project's "Scans/Day" renders as 0.00 even when projects are scanning daily, because the count is pulled from a field that does not exist on the project map. Note that collectActiveScans/addActiveProject in the SAME file correctly reads scan_count_30_days from the scans map (common.ProjectScans), proving the intended data source. The unit test maturity_test.go:190 masks this by hand-injecting "30_day_scans": 10 into the project map, which never happens in production. So tests pass while the report is wrong.
- FIX: Derive the per-tier scan counts from the scans (common.ProjectScans) data, not the project map. Change countScansByTier to take (projects, scans) and iterate scans[sid][ci][projectKey]["scan_count_30_days"], mapping each projectKey back to its project's tier (via projects[sid][projectKey]["tier"]), exactly the way collectActiveScans already walks the scans structure. Then sum those into per-tier counts before dividing by 30.
- VERDICT: CONFIRMED REAL BUG. Verified the full data flow.

Fact-check of the claim (all confirmed):
- usage.go:108 `s30, _ := project["30_day_scans"].(int)` inside countScansByTier is the ONLY production reference to the key "30_day_scans" (repo-wide grep: only other hit is maturity_test.go:190). There is NO assignment to "30_day_scans" anywhere in production code.
- Project maps are constructed in internal/report/common/projects.go: ProcessProjectDetails (lines 36-50) sets exactly server_id, name, key, main_branch, profiles, languages, quality_gate, binding, loc, tier, rules, template_rules, plugin_ru

### [MEDIUM] formatFloatCommas corrupts negative floats and drops the carry when the fractional part rounds to 1.00
- unit: report-maturity-b | file: internal/report/markdown.go:195-201 | cat: logic-error
- DESCRIPTION: formatFloatCommas does intPart := int(f) (truncation toward zero), then frac := fmt.Sprintf("%.2f", f-float64(intPart)) and decimal := frac[1:] assuming frac always starts with the leading '0' of a value in [0,1). Two defects: (1) For negative f, f-float64(intPart) is negative, e.g. f=-3.25 -> intPart=-3 -> frac="-0.25" -> frac[1:]="0.25" (it strips the minus sign, not the leading zero) -> result "-3"+"0.25" = "-30.25", a wildly wrong number. (2) Rounding carry: f=0.999 -> intPart=0 -> frac=Sprintf("%.2f", 0.999)="1.00" -> decimal=".00" -> result "0"+".00"="0.00", losing the carried 1 (should be "1.00"); similarly 5.999 -> "5.00" instead of "6.00".
- WHY: Any float64 routed through FormatValue (which calls formatFloatCommas) is mis-rendered when negative or when its fractional part rounds up to .00. The values that currently flow through FormatValue (scans_per_day, task avg/min/max queue & execution times, coverage percentages) are non-negative, so the sign defect is latent, but the rounding-carry defect is live: e.g. a coverage of 99.999% or an avg execution time of 12.999 would print as 99.00 / 12.00. FormatValue is a generic shared helper used by every report table, so any future negative or near-integer float silently renders wrong.
- FIX: Don't hand-assemble the decimal from a separately-truncated integer part. Format the whole number once and add commas only to the integer portion, e.g. s := strconv.FormatFloat(f, 'f', 2, 64); split on '.'; add commas to the integer side (preserving a leading '-'); rejoin. This keeps the sign and the rounding carry consistent.
- VERDICT: REAL bug, confirmed by execution. formatFloatCommas (internal/report/markdown.go:195-201) does intPart := int(f) then frac := fmt.Sprintf("%.2f", f-float64(intPart)) and decimal := frac[1:].

CARRY-DROP DEFECT (LIVE): When the fractional part rounds up to 1.00, the int part comes from the truncated float while "1.00" contributes only ".00", dropping the carry. I reproduced this with a completely realistic input: calcPercentage(50,21) (50 lines to cover, 21 uncovered = exactly 58% coverage) in maturity/coverage.go. The raw float64 is 57.9999..., so int(f)=57, Sprintf("%.2f", f-57)="1.00", decim

### [MEDIUM] Customized-value filter runs before the SQS-only partition, dropping SQS-only settings whose value equals the SQS default that the real migrate surfaces
- unit: predict | file: internal/predict/global_settings.go:82-129 | cat: logic-error
- DESCRIPTION: synthesizeSetGlobalSettings applies the IsSettingCustomized filter (line 91, `if !migrate.IsSettingCustomized(...) { continue }`) BEFORE consulting the SQS-only handlers (EvaluateSQSOnlyGlobalSetting / IsSilentlySkippedGlobalSetting at lines 114/118). The real migrate path deliberately does the opposite: runSetGlobalSettings calls partitionSQSOnlySettings FIRST and only then applies IsSettingCustomized (internal/migrate/tasks_setglobalsettings.go:432-444). The migrate code comment at lines 412-420 explicitly documents this ordering as a bug fix: the previous (customized-first) order "hid keys whose value happened to equal SQS's default — e.g. sonar.qualityProfiles.allowDisableInheritedRules=true where SQS's parentValue is also true: the customized filter dropped it, and the section-level note ... never reached the report."
- WHY: Concrete scenarios where the predictive report diverges from the real migrate report (which it is supposed to mirror exactly): (1) sonar.qualityProfiles.allowDisableInheritedRules with value="true" and parentValue="true" — IsSettingCustomized returns false (v==pv), so predict drops it at line 91, but the handler wants it surfaced as a Skipped/not-on-SQC note. (2) Any alwaysEnabledOnSQC key (sonar.architecture.visualization.enabled, sonar.mcp.enabled, sonar.misracompliance.enabled, sonar.sca.featureEnabled) sitting at its SQS default — the handler returns a Note for ANY value, but IsSettingCustomized=false drops it before the handler runs. The downstream default-value sweep does not recover these: it passes the getServerSettingsDefinitions record (di.Data, which has no `value` field, only `defaultValue`) to the handlers, so value-driven handlers see value="" and either return SkipSilently (suppressing the row) or a Note (line 151 EvaluateSQSOnlyGlobalSetting → isSQSOnly → continue, also suppressing). Net effect: SQS-only settings the operator left at default are silently absent from the predictive report even though the real migrate lists them as Skipped/not-on-SQC.
- FIX: Reorder to match migrate: evaluate the SQS-only handlers (IsSilentlySkippedGlobalSetting / EvaluateSQSOnlyGlobalSetting) BEFORE the IsSettingCustomized filter, so SQS-only keys are partitioned and surfaced based on their raw value regardless of whether that value equals the SQS default — then apply IsSettingCustomized only to the remaining (non-SQS-only) keys.
- VERDICT: CONFIRMED real bug; I could not refute it and reproduced it end-to-end. Files: internal/predict/global_settings.go:82-129 (predict) vs internal/migrate/tasks_setglobalsettings.go:412-444 (migrate).

Ordering is genuinely reversed. Migrate's runSetGlobalSettings calls partitionSQSOnlySettings FIRST (line 432), then the IsSettingCustomized filter (440). The comment at 412-420 plus the dedicated regression test TestRunSetGlobalSettingsSQSOnlyNoteFiresEvenAtSQSDefault (tasks_setglobalsettings_test.go:1319) document this exact ordering as a #200 bug fix. Predict's synthesizeSetGlobalSettings does t

### [MEDIUM] checkProjectCount measures SQS and SC counts with two different methods; SC count silently caps at one page
- unit: regtest | file: internal/regtest/checks.go:69-80 | cat: pagination
- DESCRIPTION: The SQS side calls queryTotal (which reads paging.total, the true server-reported count) while the SC side calls queryCount with the 'components' result key and NO page-size parameter. queryCount (helpers.go:40-50) does a single raw.Get and returns len(items) for whatever the endpoint returned on that one page. api/projects/search defaults to ps=100 (max 500), so the SC count is len(components) on page 1 only and is hard-capped at 100.
- WHY: For any SonarCloud organization with more than 100 migrated projects, scCount is capped at 100 while sqsCount is the real total (e.g. 250). The check makeResult compares sqsCount==scCount and reports FAIL ('Exact') even when every project migrated correctly. Conversely, because the two sides use fundamentally different counting methods (true total vs single-page array length), the check cannot reliably detect a genuine project-count shortfall either. The extract package always paginates api/projects/search (internal/extract/tasks_projects.go) and uses ps up to 500 elsewhere, confirming >100-project instances are an expected, supported scenario.
- FIX: Count both sides symmetrically. Either use queryTotal for the SC side too (api/projects/search returns paging.total on SonarCloud) e.g. queryTotal(ctx, s.scRaw, "api/projects/search", urlParams("organization", s.cfg.SCOrg)), or paginate via s.scRaw.GetPaginated and count the result. queryTotal is the simplest symmetric fix.
- VERDICT: The claim is verified against the actual code. checkProjectCount (internal/regtest/checks.go:69-80) measures the two sides with incompatible methods: the SQS side calls queryTotal (helpers.go:27-37), which sets ps=1 and returns paging.total — the true server-reported count; the SC side calls queryCount (helpers.go:40-50) which does a single raw.Get with only {organization} (no ps param) and returns len(components). I confirmed there is no hidden pagination: RawClient.Get -> doGet is one HTTP request (rawclient.go:97-106, 194-221); only GetPaginated loops, and queryCount does NOT use it. SonarC

### [MEDIUM] buildRunInfo unmarshals extract.json into map[string]string, so SourceURL is never populated in the history list
- unit: gui | file: internal/gui/history.go:67-72 | cat: logic-error
- DESCRIPTION: buildRunInfo reads extract.json and does `var meta map[string]string; if json.Unmarshal(data, &meta) == nil { ri.SourceURL = meta["url"] }`. As established, extract.json contains numeric (`version`) and array (`target_tasks`, `available_configs`) values, so json.Unmarshal into map[string]string returns a non-nil error. The `== nil` guard is therefore false and the body never runs, leaving ri.SourceURL empty.
- WHY: Every RunInfo returned by fetchRunList / listRuns / RunsListHandler will have an empty SourceURL even though extract.json contains a valid "url" field. The history list (and /api/runs JSON) silently drops the source URL for all runs. (Interestingly Go's decoder does populate string-valued keys before hitting the first non-string value and erroring, so the data is partially there in the map, but the `== nil` gate discards it entirely.) Confirmed via standalone repro: the map gets url="..." populated but Unmarshal still returns an error, so the guard rejects it.
- FIX: Decode extract.json into map[string]any or a typed struct and read meta["url"] (coercing to string), rather than gating on a clean unmarshal into map[string]string. Mirror internal/structure/extract.go which decodes into `struct{ URL string `json:"url"` }`.
- VERDICT: CONFIRMED real bug. I verified every link in the chain.

1. Real extract.json contents: writeMetadataFile (internal/extract/extract.go:357-378) writes a map[string]any with non-string values: "version": m.Version.LegacyFloat() (a float, e.g. 2026.3) and "plan"/"target_tasks"/"available_configs" as arrays. I inspected an actual on-disk file (go/migration-files/2026-06-05-11/extract.json) and confirmed it contains `"version": 2026.3`, plus array-valued `plan`, `target_tasks`, `available_configs`.

2. Go semantics (empirically reproduced against the real file with go1.26.3): `var meta map[string]

### [MEDIUM] Failure error_message column is silently blank for any non-{errors:[...]} response body
- unit: analysis | file: internal/analysis/analysis_report.go:199-224 | cat: data-loss
- DESCRIPTION: extractErrorMessage -> extractErrorsFromValue only returns a non-empty string when the failure response is (or JSON-parses to) an object containing an `errors` array of `{"msg": ...}` objects. For any other failure body it returns "": (1) a non-JSON string body such as a 500 plain-text 'Internal Server Error' or an HTML '502 Bad Gateway' page, and (2) a JSON object that has no `errors` key, e.g. the SonarQube Cloud v2 API error shape `{"message":"Insufficient privileges"}`. The v2 shape is used by exactly the endpoints this report classifies (`/enterprises/portfolios` and `/dop-translation/project-bindings`). The result is that the final_analysis_report.csv error_message column — the column whose entire purpose is to tell the operator WHY a request failed — is empty precisely for the most opaque failures (5xx, gateway errors, v2-API permission errors).
- WHY: The three sibling failure collectors that parse the identical requests.log (internal/report/summary/partial.go:204-228 extractFailureError, plus portfolio_failures.go and project_failures.go which call it) DO preserve this text: for a non-JSON string body they return the raw string `v`, and for a JSON string that parses but has no `errors` array they fall through to `return v`. I reproduced the divergence: input 'Internal Server Error' -> analysis="" but sibling="Internal Server Error"; input '{"message":"Insufficient privileges"}' -> analysis="" but sibling returns the raw JSON. So on the same log line, the analysis report drops diagnostic text that the summary report keeps. Trigger: any migration POST that fails with a 5xx, a gateway/proxy error page, or a v2-API endpoint error — all common in real runs (rate limiting, transient SQC errors, portfolio/project-binding permission denials).
- FIX: Mirror the sibling extractFailureError logic in extractErrorsFromValue: in the `string` branch, after json.Unmarshal, if joinErrorMsgs(parsed) is empty (or the body did not parse as JSON at all), return the raw string `v` instead of "". Optionally also handle the `map[string]any` branch with no `errors` key by surfacing a `message` field if present. This makes the analysis report's error column consistent with the summary collectors and stops swallowing 5xx/HTML/v2-API error text.
- VERDICT: CONFIRMED REAL. I attempted to refute the claim and could not; every element holds up under code reading.

The divergence is real and unguarded. In /go/internal/analysis/analysis_report.go, extractErrorsFromValue (210-224) -> joinErrorMsgs (226-244) returns "" unless the body is/parses to an object with an `errors` array of `{"msg":...}`. The sibling /go/internal/report/summary/partial.go extractFailureError (204-228) has an extra `return v` fallthrough: for a non-JSON string it returns the raw string, and for JSON that lacks `errors` it returns the raw JSON `v`. project_failures.go:137 and po

### [MEDIUM] regtest config loader only parses the side-sectioned shape, rejecting the flat and unified migrate configs it claims to accept
- unit: cmd-aux | file: internal/regtest/suite.go:249-289 | cat: api-misuse
- DESCRIPTION: regtestCmd's help text states the --config flag takes a JSON file in the "same format as extract/migrate", and cmd/regtest.go validates the loaded Config requiring SQSURL/SQSToken/SCURL/SCToken/SCOrg. But regtest.LoadConfigFile (internal/regtest/suite.go) unmarshals into a struct that ONLY recognizes the side-sectioned shape: it reads raw.SonarQube.{url,token} and raw.SonarCloud.organizations[0].{url,token,key}. The migrate command actually accepts four documented shapes (see internal/migrate/config_file.go: flat, command-sectioned `migrate`, side-sectioned `sonarcloud`, and unified `source`/`target`). The flat migrate example (examples/config-migrate.example.json) puts the SonarCloud target under top-level `url`/`token`, and the unified example (examples/config.unified.example.json, the #266-recommended shape) puts it under a `target` block with no `sonarqube`/`sonarcloud` keys at all. For both of those shapes LoadConfigFile leaves every field empty.
- WHY: A user who has a valid, complete migrate config in the flat or unified shape (which the docs and the regtest help text say is supported) runs `regtest --config config.json`. LoadConfigFile silently returns a zero-valued Config (SQSURL="", SCURL="", etc.) because none of the expected JSON keys exist in those shapes. cmd/regtest.go then fails validation with "SonarQube Server URL and token are required (provide via config file)" even though the credentials are present in the file. The regression suite cannot be run against the exact config that drove the migration, contradicting its documented contract.
- FIX: Reuse the shared migrate/extract multi-shape parser (e.g. extract.parseConfigFile / the configFileShape detection in internal/migrate/config_file.go) to populate SQSURL/SQSToken from the source/sonarqube/flat blocks and SCURL/SCToken/SCOrg from the target/sonarcloud blocks, instead of regtest's bespoke single-shape struct. At minimum, also read the flat top-level url/token and the unified source.{url,token} / target.{url,token,organization_key} so all documented shapes resolve.
- VERDICT: CONFIRMED REAL. regtest.LoadConfigFile (internal/regtest/suite.go:255-288) unmarshals into a struct recognizing ONLY the side-sectioned shape with the NESTED organizations form: SQS from `sonarqube.{url,token}`, SC from `sonarcloud.organizations[0].{url,token,key}`, settings from `settings.{export_directory,project_keys}`. The migrate command (internal/migrate/config_file.go:23-222) documents and parses four shapes. Mapping each official example against the regtest parser produces a failing path:

(1) config.unified.example.json + config-transfer.example.json (unified source/target, the #266-r


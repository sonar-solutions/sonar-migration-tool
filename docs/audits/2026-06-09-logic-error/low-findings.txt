### [LOW] Cross-branch issues that collide on the same Cloud line can be processed concurrently against the same Cloud issue, double-posting comments and racing SetTags
- unit: migrate-issuesync | file: internal/migrate/tasks_issuesync.go:508-526, 646-661, 700-728, 746-759 | cat: concurrency
- DESCRIPTION: Because the actionable set is not branch-scoped (see related finding) and the Cloud search has no branch param, two source issues (e.g. one on main, one on a feature branch) on the same file+line+rule both resolve to the SAME single Cloud candidate. syncProjectIssues runs up to 20 goroutines over the actionable slice, so both can call syncOnePair on the identical cloud key concurrently. The idempotency guard slices.Contains(pair.cloud.Tags, metadataSyncTag) reads the Tags captured at search time (before either goroutine ran), so both pass it. The comment-dedup in isAlreadyMigratedIssueComment compares against pair.cloud.Comments, also captured at search time and not refreshed, so the just-added comment is invisible to the second goroutine — both AddComment the same comment, producing duplicates. Concurrent SetTags calls also race (last-writer-wins on the Cloud tag list).
- WHY: When source branches share a file/line/rule (common for shared base code across branches), the same Cloud issue is touched twice in parallel, yielding duplicate migrated comments and a non-deterministic tag set. The behavior depends on map/slice iteration ordering and goroutine scheduling, so it is intermittent.
- FIX: Deduplicate the actionable set by resolved Cloud issue key before dispatch, or serialize sync per Cloud issue key (e.g. group actionable sources by candidate key and process each group sequentially). Branch-scoping the search (per the related finding) also removes most of the collisions.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] collectBranchInfo failure is indistinguishable from 'no branches', silently collapsing a multi-branch project to main-only
- unit: migrate-projectdata | file: internal/migrate/tasks_projectdata.go:74-79, 502-527 | cat: error-handling
- DESCRIPTION: collectBranchInfo returns nil both when the getBranches extract genuinely has no LONG branches AND when readExtractItems errors (lines 503-506 return nil on err). runImportProjectData then treats len(sqBranches)==0 as 'synthesize a single main branch' (lines 75-77) and imports only main.
- WHY: If the getBranches extract is present but transiently unreadable (corrupt file, IO error), a project that actually has several long-lived branches is migrated as main-only with no warning. The error inside collectBranchInfo is swallowed and the empty result is interpreted as the legitimate single-branch case, so the non-main branches are dropped without any surfaced error.
- FIX: Return an error (or at minimum log a Warn) from collectBranchInfo on the readExtractItems failure path so the caller can distinguish 'no branches in extract' from 'failed to read branch extract' and avoid silently importing only main when branch data was expected but unreadable.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Composite lookup keys built by separator-less string concatenation can collide
- unit: migrate-associate | file: internal/migrate/tasks_associate.go:129-137, 175 (and 195-203, 214) | cat: logic-error
- DESCRIPTION: profileLookup is keyed by orgKey+lang+name with no delimiter (line 136), gateLookup by orgKey+name (line 202), and the project lookups by server+srcKey (e.g. line 156). Because the components are concatenated directly, two distinct tuples whose concatenations are equal map to the same bucket. Example: (org="a", lang="bc", name="d") and (org="ab", lang="c", name="d") both produce "abcd". A collision would cause profileLookup[target.orgKey+lang+name] to report a profile as migrated (or not) for the wrong org/language, leading to a wrong or missing add_project call.
- WHY: In a multi-org migration, an org key that is a prefix of another org key combined with the right language/name boundary can make the lookup return a positive (or negative) match for the wrong entity, silently assigning or skipping a quality-profile/gate binding. The probability is low because org keys and languages are constrained slugs, but it is a real correctness hazard, not style.
- FIX: Join components with a delimiter that cannot appear in any component, e.g. orgKey+"\x00"+lang+"\x00"+name (the file already uses \x00 as a separator for link/webhook dedup keys at lines 1000/1120). Apply the same to gateLookup and the server+key project keys.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Default-value sweep misreports a merged-away global exclusion key as "left to default, no migration needed"
- unit: migrate-settings | file: internal/migrate/tasks_setglobalsettings.go:587-633 (interacts with 469-471 and 1018-1037) | cat: logic-error
- DESCRIPTION: When the operator HAS set sonar.global.exclusions (or sonar.global.test.exclusions) on SQS, mergeGlobalIntoLocal folds its patterns into the local sibling key (sonar.exclusions / sonar.test.exclusions) and REMOVES the global key from the `customized` slice (line 1023: `case pair.SQSGlobalKey: continue`). The `customizedKeys` map at lines 587-592 is built from the post-merge `customized` slice, so it contains the LOCAL key but NOT the global key. The default-value sweep (lines 597-633) then iterates getServerSettingsDefinitions, finds sonar.global.exclusions present as a definition key (verified present in migration-files/*/getServerSettingsDefinitions), sees it is not in customizedKeys, not in partitionHandled, not an AI key, and resolveSQSOnlyHandler returns not-found — so it emits a section-level Skipped row with Detail = skipDetailDefaultValue ("Setting is left to default on SQS, no migration needed").
- WHY: The global key's patterns WERE migrated (merged into the local key, which gets its own 'Applied ... (merged from sonar.global.exclusions + sonar.exclusions)' row), yet the same report ALSO shows 'sonar.global.exclusions: Setting is left to default on SQS, no migration needed.' The two rows directly contradict each other. This only triggers in exactly the case where migration happened (global exclusions actually set), so every customer who uses platform-enforced global exclusions sees the misleading/contradictory row. The actual API migration is correct; only the report is wrong.
- FIX: Exclude merged-away global keys from the default-value sweep. Either (a) record the set of SQSGlobalKey names consumed by mergeGlobalIntoLocal and add them to an exclusion set the sweep consults, or (b) in the sweep loop add `for _, pair := range globalExclusionPairs { if key == pair.SQSGlobalKey { continue } }` guarded by whether the global key was actually present/set. The cleanest fix is to have mergeGlobalIntoLocal return the names of global keys it folded and add them to `customizedKeys`/`partitionHandled` so the sweep skips them.
- NOTE: CONFIRMED at runtime with a reproduction test. Execution path: (1) When sonar.global.exclusions is set non-default on SQS, it passes IsSettingCustomized (line 440) and enters `customized`; (2) mergeGlobalIntoLocal (called at line 469-471) folds its patterns into sonar.exclusions and REMOVES the glob

### [LOW] Fan-out "no eligible projects" path marks the row Failed but never increments the failure counter and leaves Reason empty
- unit: migrate-settings | file: internal/migrate/tasks_setglobalsettings.go:833-877 (fanOutOutcome) with 813-819 | cat: logic-error
- DESCRIPTION: In fanOutOutcome, after fanOutGlobalToProjects returns, counter.Success()/counter.Fail() are incremented only per applied/failed project (lines 814-819). In the default switch branch where a==0 and f==0 ("Failed: no eligible projects in org", line 848) the status is set to outcomeFailed (line 850) but no counter.Fail() is ever called, and out.Reason is only populated when f>0 (line 872), so the Failed report row has an empty ErrorMessage.
- WHY: A setting can be routed to the Failed bucket of the report while the task's success/failure summary counter shows zero failures for it — an inconsistency between the rendered report and the TaskCounter summary. The Failed row also carries no Reason, so the report's EntityItem.ErrorMessage is blank. Reachability is low (an org appearing in projectDefsByOrg generally implies at least one project in projectKeyMap for that org), which is why this is low severity.
- FIX: In the a==0 && f==0 branch, either downgrade the status to skipped ("no eligible projects in org" is not a real failure) with a clear Reason, or call counter.Fail() once and set out.Reason to a fixed string so the counter and report agree. Treating it as skipped is the more accurate semantics.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Merge synthesizes a values-only record, silently dropping fieldValues if the local exclusion key were a property-set
- unit: migrate-settings | file: internal/migrate/tasks_setglobalsettings.go:1011-1016 (mergeGlobalIntoLocal) with 1050-1061 (readPatterns) | cat: data-loss
- DESCRIPTION: mergeGlobalIntoLocal always builds the synthesized record with only key/values/_merged_from (lines 1011-1015) and readPatterns only inspects `values`/`value` (lines 1050-1061). If the local key (pair.SQSLocalKey) had been customized as a PROPERTY_SET (fieldValues), those fieldValues are not read and are dropped from the synthesized record that replaces the original in the `customized` slice.
- WHY: For the two current pairs (sonar.exclusions, sonar.test.exclusions) these are always pattern lists, never property-sets, so this is not exploitable today. But the exclusionPair mechanism is documented as extensible ("More pairs can be added without touching the merge logic; just append to globalExclusionPairs"), and a future pair whose local key is a property-set would silently lose its fieldValues on merge. The merge also unconditionally posts the union as `values=`, which would be the wrong shape for a property-set key.
- FIX: Either document/assert that exclusionPair entries must be pattern-list (multi-value) keys, or have mergeGlobalIntoLocal carry through fieldValues (read via extractObjectArray on localRec) when present and synthesize the appropriate shape. At minimum guard against adding a property-set key to globalExclusionPairs.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] already-exists lookups for gates and permission templates read only one unpaginated page
- unit: migrate-create | file: internal/migrate/create_lookup.go:44-55, 76-87 | cat: pagination
- DESCRIPTION: lookupExistingGate (GET /api/qualitygates/list) and lookupExistingTemplate (GET /api/permissions/search_templates) call raw.Get, which performs a single GET with no p/ps params and returns only the first page of results. findByField/findByFieldFold then scan that single page for an exact (or case-folded) name match and return 'not found' if absent. If the organization has more gates/templates than fit on the server's default first page, the exact match can sit on a later page and never be seen.
- WHY: When createGates / createPermissionTemplates hit the IsAlreadyExists branch (re-run, reset, or a name that already exists on SQC), the lookup is the only way to recover the existing entity's ID. If that ID is on page 2+, lookupExistingGate/Template returns the synthetic 'not found' error, counter.Fail() fires, the item is dropped from the task output JSONL, and downstream tasks (addGateConditions which clears+repopulates conditions for pre-existing gates; setDefaultGates/Templates) silently never run for that gate/template. The migrated quality gate ends up without its conditions. Unlike lookupExistingProfile (filtered by qualityProfile+language+organization) and lookupExistingGroup (filtered by q=name), these two endpoints take no query filter, so they return the full org-wide list and are the most exposed to overflowing the first page.
- FIX: Switch lookupExistingGate and lookupExistingTemplate to RawClient.GetPaginated (with the appropriate ResultKey, e.g. "qualitygates" / "permissionTemplates"), or at minimum set a large ps (e.g. 500) on the params passed to raw.Get, so the exact-name scan sees every gate/template rather than only the first page. Note these endpoints may not advertise a paging object; if so, set ps explicitly and verify the response size against the org's entity count.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] setOrgGroupPermissions does not skip sonar-administrators, generating 404 noise and risking grants to an unintended group
- unit: migrate-permissions | file: internal/migrate/tasks_permissions.go:240-255 | cat: api-misuse
- DESCRIPTION: runSetOrgGroupPermissions only special-cases the `Anyone` group (line 243) and remaps sonar-users via MapGroupNameToCloud. It does NOT skip `sonar-administrators`, the SonarQube Server built-in admin group. MapGroupNameToCloud("sonar-administrators") returns ("sonar-administrators", true) because that name is not in builtInGroupCloudAliases, so the code attempts e.Cloud.Permissions.AddGroup("sonar-administrators", perm, orgKey, ""). By contrast, runSetTemplateGroupPermissions (line 379-383) explicitly lists `sonar-administrators` in skipGroups, and its own doc comment (lines 333-335) states sonar-users/sonar-administrators have no SQC equivalent accessible via API. This is an asymmetry: the template path skips it, the org path does not.
- WHY: On SonarQube Cloud there is no `sonar-administrators` group by default, so each global permission grant for that group 404s and is counted as counter.Fail() with a warning — inflating the failure count and noise. If createGroups happened to create a same-named custom group on SQC, the org permissions of the SQS built-in admin group could be applied to it unintentionally. Either way the behavior diverges from the documented intent that sonar-administrators is not migratable.
- FIX: Apply the same skip used by the template path in runSetOrgGroupPermissions: skip the grant when the group name is `sonar-administrators` (and keep the existing `Anyone` skip). Centralizing the built-in skip list (e.g. an IsNonMigratableBuiltIn helper) would keep the org and template paths consistent.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] setProfileGroupPermissions joins profile-group rows to profile refs by source key without disambiguating server URL
- unit: migrate-permissions | file: internal/migrate/tasks_permissions.go:280-313 | cat: logic-error
- DESCRIPTION: runSetProfileGroupPermissions builds profileInfo keyed solely by `source_profile_key` aggregated across ALL createProfiles output rows (which can span multiple source servers in a multi-server migration). The per-row extract item carries its originating server (item.ServerURL via forEachExtractItem) but the join `refs := profileInfo[profileKey]` ignores it. If two different source servers each have a quality profile whose SQS profile key (UUID) collides, the group permission from one server's profile would be applied to BOTH servers' migrated profiles.
- WHY: SonarQube profile keys are server-local identifiers; in a consolidation migration pulling several SQS servers into one SQC enterprise, a key collision (or, more plausibly, two profiles sharing an identical-looking key in fixtures/restored backups) causes a profile-group grant intended for one org's profile to be replayed against another org's profile of the same key. The other per-server tasks (setProjectGroupPermissions, setProjectSettings) defensively scope their lookups with item.ServerURL+key; this one does not, breaking that invariant. Real-world collision probability is low (UUID keys), hence low severity/confidence, but the missing server scoping is a genuine correctness gap relative to the sibling tasks.
- FIX: Key profileInfo by server URL + source_profile_key (the createProfiles rows carry `server_url`), and look up with item.ServerURL+profileKey in the apply closure, matching the pattern used by runSetProjectGroupPermissions / runSetProjectSettings.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] TAGS portfolio with an empty tag list issues a PATCH with selection=tags but no tags
- unit: migrate-portfolios | file: internal/migrate/tasks_portfolios.go:476-481, 123-159 | cat: api-misuse
- DESCRIPTION: When parentMode == "TAGS" and parentTags == "", resolveEffectivePortfolioConfig returns ("TAGS", "", nil) at line 479. Back in runConfigurePortfolios the TAGS branch (lines 134-136) sets params.Selection = "tags" and params.Tags = nil. buildPortfolioPatchBody (enterprises.go lines 132-134) only emits `tags` when len(params.Tags) > 0, so the PATCH carries selection:"tags" with no tags field.
- WHY: SQC validates selection/criterion pairing (cf. the real API error 'selection regex requires regularExpression' captured in collect_test.go line 709). A selection=tags PATCH with an absent tags list will be rejected by the API, producing a per-portfolio failure for a portfolio that has a TAGS mode but an (extract-time) empty tag string — instead of being skipped or left at its default. The TAGS combine path (lines 524-526) already guards this with `if len(union) == 0 { return "", "", nil }`, but the single-portfolio path at lines 477-480 does not.
- FIX: In the parentMode == "TAGS" branch, treat an empty tag list the same as the union path: `if parentTags == "" { return "", "", nil }` so no PATCH is issued, rather than returning ("TAGS", "", nil).
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Intentionally-dropped gate condition (no SQC equivalent) is counted as a task failure
- unit: migrate-configure | file: internal/migrate/tasks_configure.go:173-185 | cat: logic-error
- DESCRIPTION: In runAddGateConditions, when a source metric is known to the mapping table but has no SonarQube Cloud equivalent (LookupMetricReplacement returns mapped==true, len(targets)==0), the code records a 'dropped' note and then calls counter.Fail() before `continue`. This is the designed, expected outcome for metrics like contains_ai_code, prioritized_rule_issues, releasability_rating, etc. — it is not an API error or a migration failure.
- WHY: TaskCounter.Fail() feeds the end-of-task 'task summary' log (succeeded/failed/total via LogSummary in helpers.go) and the migration summary report. A source gate that legitimately contains, say, two metrics with no SQC equivalent will report 'failed: 2' even though every API call that actually fired succeeded. Operators reading the summary cannot distinguish a real CreateCondition HTTP failure (line 248) from a benign, by-design drop, so the failure count is no longer a reliable signal and triggers false investigation. The drop is already surfaced correctly via the Warn log and the sidecar 'dropped' note; double-marking it as a Fail corrupts the success/failure accounting.
- FIX: Remove the counter.Fail() call at line 183 for the no-equivalent drop case (keep the Warn log and the recordGateConditionNote 'dropped' entry). If a 'skipped/dropped' tally is wanted, add a dedicated counter that is reported separately from genuine API failures rather than reusing Fail().
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Project DOP binding relies on a numeric 'id' field from /api/projects/search that may be absent
- unit: migrate-configure | file: internal/migrate/tasks_alm.go:71-101 | cat: api-misuse
- DESCRIPTION: runMatchProjectRepos reads projID := extractField(proj, "id") from getProjectIds output (the components array of /api/projects/search) and writes it as project_id; runSetProjectBinding then skips the binding when projID=="" (line 101) and otherwise passes it as DOP ProjectBindingParams.ProjectID (the internal numeric Cloud project id). SonarCloud's /api/projects/search components do not reliably expose an internal numeric `id` field (key/name/qualifier/visibility/lastAnalysisDate are the documented fields).
- WHY: If /api/projects/search does not return an `id` for components in the target SonarCloud edition, extractField(proj, "id") yields "", matchProjectRepos still emits a record but with project_id="", and runSetProjectBinding silently skips it at line 101 — so ALM/DevOps bindings are quietly never created for any project, with no error surfaced (only the absence of a binding). Even if a record is emitted, an empty/non-numeric ProjectID would make the dop-translation POST fail.
- FIX: Verify the actual field name SonarCloud returns for the internal project id on /api/projects/search for the relevant edition; if the numeric id is only available via a different endpoint (e.g. project navigation/component or a dedicated lookup), source project_id from there. At minimum, log a Warn when a cloud-bound project matched a repo but yielded an empty project_id, so a silently-skipped binding is observable.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] apiURL derivation silently yields the wrong host for any non-https:// cloud URL
- unit: migrate-planner | file: internal/migrate/migrate.go:101-102 | cat: api-misuse
- DESCRIPTION: apiURL is derived as `strings.Replace(cloudURL, "https://", "https://api.", 1)`. This only injects the `api.` subdomain when the URL begins with the literal `https://`. For any other scheme (e.g. an operator-supplied `http://...` target_url, or a URL with a leading space/uppercase scheme), the Replace matches nothing and apiURL ends up identical to cloudURL. The enterprise client (apiClient/apiCC/rawAPI), which must talk to api.<host>, is then pointed at the standard host instead.
- WHY: target_url is fully operator-controlled (cmd/transfer.go:153, cmd/migrate.go) with no scheme validation before this point. A user who passes a non-https URL gets every enterprise-API call (org existence checks, enterprise endpoints) silently routed to the standard host, producing confusing 404s/permission errors rather than a clear configuration failure. The transform is brittle string surgery rather than proper URL host manipulation.
- FIX: Parse cloudURL with net/url and prepend `api.` to the Host explicitly, or validate that the URL starts with https:// and error out otherwise. At minimum, fall back / warn when the Replace produced no change (apiURL == cloudURL) so the misconfiguration is visible.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] primary.Suggestions.Detail accumulates dropped-project suffixes across orgs, polluting later orgs' report rows
- unit: migrate-misc | file: internal/migrate/ai_codefix.go:412-417 | cat: data-loss
- DESCRIPTION: Inside the per-org loop, when droppedProjects>0 the code mutates `primary.Suggestions.Detail` by appending '%d project(s) skipped …'. `primary` is a local copy but its Suggestions field (and Detail string) is reused for every org and re-read by appendOrgOutcome(&suggRec, org, primary.Suggestions, …) on line 421. Because the append is cumulative on the same Detail, each org after the first inherits the concatenated 'skipped' notes from all previous orgs.
- WHY: Org A drops 2 projects -> primary.Suggestions.Detail becomes '<base> 2 project(s) skipped …'. appendOrgOutcome snapshots that for A's row (correct). Org B drops 3 -> Detail becomes '<base> 2 project(s) skipped … 3 project(s) skipped …', and B's report row is written with BOTH counts. The user-facing migration report shows org B as having skipped 2+3 projects, misreporting the per-org skip count.
- FIX: Don't mutate the shared primary.Suggestions. Build the per-org detail in a local copy of the row: `sugg := primary.Suggestions; if droppedProjects>0 && sugg.Status==outcomeApplied { sugg.Detail = fmt.Sprintf(...) }` then pass `sugg` to appendOrgOutcome.
- NOTE: The bug is REAL. In /Users/joshua.quek/Desktop/agents-sonar-migration-tool/sonar-migration-tool/go/internal/migrate/ai_codefix.go, `primary` is a struct value declared once at line 365 (`primary := pickPrimaryDecision(decisions)`) OUTSIDE the `for _, org := range orgList` loop, and is never reassign

### [LOW] extractType parameter is plumbed through but never used in task selection
- unit: extract-core | file: internal/extract/planner.go:118-134 | cat: logic-error
- DESCRIPTION: targetTasks accepts extractType but never references it in the body — only targetTask and includeProjectData affect the result. ExtractConfig.ExtractType is plumbed from the CLI/config (defaulting to "all" in applyDefaults) all the way into buildPlan -> TargetTasks/TargetTasksWithProjectData -> targetTasks, but has zero effect on which tasks are selected.
- WHY: If a user supplies a non-"all" --extract_type value expecting it to scope the extraction to a specific report type/subset (the field's documented purpose: 'all or report type'), the value is silently ignored and a full extract runs anyway. The behavior diverges from the configuration the user provided. This is only a no-op (not wrong output) for the default "all" value, hence low severity, but any documented non-default value produces surprising behavior.
- FIX: Either implement extractType filtering (map a report-type value to the corresponding task subset and intersect with the discovered get* tasks) or remove the parameter and reject/ignore non-"all" values explicitly with a clear error/log so users aren't misled into thinking it filters.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Top-level skip_project_data_migration is ignored when an 'extract' sub-object is present (shape 2)
- unit: extract-core | file: internal/extract/config_file.go:123-180 | cat: logic-error
- DESCRIPTION: In toExtractConfig, the `case s.Extract != nil` branch (line 163-164) returns s.Extract.toExtractConfig() and discards every top-level field of the outer object, including SkipProjectDataMigration. The flat/default branch and the unified/side-sectioned branches all read SkipProjectDataMigration, but a shape-2 file that sets skip_project_data_migration at the top level alongside an `extract` block will have that flag silently dropped (only extract.skip_project_data_migration is honored).
- WHY: A user writing the command-sectioned config (config.example.json with an `extract` sub-object) who sets skip_project_data_migration at the top level rather than inside the extract block gets the opposite behavior they intended: heavy project-data tasks (issues, source, SCM blame) run when they asked to skip them, or vice versa. No error or warning is emitted. Low severity because the documented placement for shape 2 is inside the extract block, but the silent precedence mismatch is a real config-resolution footgun.
- FIX: Before recursing in the s.Extract branch, merge relevant top-level flags into the nested config (e.g. if the nested SkipProjectDataMigration is false but the top-level is true, propagate it), or document/validate that command-sectioned files must place the flag inside the extract block and warn when a stray top-level flag is detected.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Extract task name getProjectFixedIssueTypes does not match the getProjectResolvedIssueTypes folder the maturity report reads, yielding an empty 'resolved issues' section
- unit: extract-tasks-a | file: internal/extract/tasks_issues.go:45-46 | cat: api-misuse
- DESCRIPTION: tasks_issues.go defines the resolution-filtered issue-count task as getProjectFixedIssueTypes (the folder/JSONL output is written under that name). The maturity report consumer reads the 'resolved' issue data from a folder named getProjectResolvedIssueTypes (internal/report/maturity/issues.go:18 folderMapping: "resolved": "getProjectResolvedIssueTypes"), and the maturity tests reference the same getProjectResolvedIssueTypes name. No extract task ever produces a getProjectResolvedIssueTypes folder, and nothing consumes getProjectFixedIssueTypes (only progress.go:115 lists its weight). The two names are permanently out of sync.
- WHY: Run `report` (maturity report) after an extract. processIssues iterates folderMapping and calls common.ReadDataParsed(dir, mapping, "getProjectResolvedIssueTypes"), a directory that does not exist in the export. The resolved-issues counts come back empty, so the maturity report's resolved/fixed issue section is silently blank even though the extract phase spent time fetching that data into getProjectFixedIssueTypes. This is a real data-contract mismatch with a user-visible wrong-output consequence (the report is missing a whole category of data).
- FIX: Make the names match. Either rename the extract task to getProjectResolvedIssueTypes (makeExpandedIssueTask("getProjectResolvedIssueTypes", resolutions, nil) and update progress.go and extract_test.go), or change the report's folderMapping value (issues.go:18) and the maturity tests to getProjectFixedIssueTypes. Pick one canonical name and use it on both the producer and consumer sides.
- NOTE: The reviewer's core factual claim is CORRECT and verified: the extract task is named "getProjectFixedIssueTypes" (tasks_issues.go:45-46), and the folder is created under that exact name by DataStore.Writer(taskName) -> filepath.Join(baseDir, taskName) (common/store.go:39-40). The maturity report's f

### [LOW] projectTagsTask swallows ALL errors (not just 403/404), silently dropping tags on transient/server failures
- unit: extract-tasks-b | file: internal/extract/tasks_projects.go:95-99 | cat: error-handling
- DESCRIPTION: Inside projectTagsTask, the per-project fetch `e.Raw.Get(ctx, "api/components/show", ...)` returns `nil` on ANY error (the comment says 'best-effort'). This bypasses the non-fatal HTTP error classification used everywhere else in the extract layer (perProjectArray/perProjectSingle/perProjectPaginated all call isNonFatalHTTPErr and only skip 403/404, returning the error otherwise).
- WHY: If api/components/show returns a transient 500/502/503, a network timeout, or a 401 (expired token mid-run), the tags for that project are silently dropped and the project is NOT even recorded as skipped. The migration then proceeds as if the project genuinely had no tags, losing data without any error surfaced. A whole-run auth failure (401) would also be masked here while every other task would correctly fail loudly. Because tags-absent and fetch-failed are indistinguishable in the output, the user has no signal that data was lost.
- FIX: Mirror the established pattern: on error, `if isNonFatalHTTPErr(err) { e.Logger.Warn(...); e.RecordSkipped(key); return nil }; return err`. Only swallow 403/404, propagate everything else.
- NOTE: The core divergence the reviewer identified is REAL and verifiable. In internal/extract/tasks_projects.go:95-99, projectTagsTask does `if err != nil { return nil }`, swallowing every error class with no log and no e.RecordSkipped(). Every sibling per-project task in internal/extract/helpers.go (perP

### [LOW] projectBindingsTask swallows ALL errors, masking non-404 failures
- unit: extract-tasks-b | file: internal/extract/tasks_projects.go:182-186 | cat: error-handling
- DESCRIPTION: projectBindingsTask returns `nil` on any error from `api/alm_settings/get_binding`, with the comment 'Binding may not exist; skip gracefully.' A missing binding is the legitimate 404 case, but this also swallows 500s, timeouts, and auth failures.
- WHY: A binding that exists but fails to fetch due to a transient 5xx or a 401 mid-run will be silently treated as 'no binding', so the project's ALM/DevOps binding is dropped from the export and never migrated. Unlike the standard helpers, this does not distinguish 404 (expected absence) from real failures, and does not record the project as skipped, so the loss is invisible.
- FIX: Check `isNonFatalHTTPErr(err)` (or specifically 404) before swallowing: return nil only for 404 (binding genuinely absent), and return the error for other status codes so real failures fail the task or are recorded.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Safety-split override keyed by issue Key collides for external issues (all have empty Key)
- unit: scanreport | file: internal/scanreport/backdate.go:112-136, 197-200 | cat: logic-error
- DESCRIPTION: buildSafetySplitOverrides stores synthetic per-batch dates as overrides[iss.Key]=syntheticDate, and applyIssueDates reads them back via overrides[iss.Key]. External issues are converted to ExtractedIssue with an empty Key (extIssuesToExtracted in tasks_projectdata.go never sets Key), so every external issue shares the key "". The extracted slice passed to BackdateChangesets mixes native issues (real keys) and external issues (Key==""). When a single calendar day has more than issueBatchSize (5000) findings and external issues fall into more than one batch, each batch write does overrides[""]=syntheticDate, so only the last write survives the map. Then applyIssueDates makes EVERY external issue read that single overrides[""] value, ignoring which batch it actually belonged to — defeating the per-batch split and assigning the wrong synthetic date to all but the issues in the last-written batch.
- WHY: Large legacy projects commonly cluster thousands of findings on the same first-analysis creation date. If >5000 same-day findings include external issues, the split into N batches collapses for externals because they all key on "": the map keeps one synthetic date, so all external issues get backdated to the same (likely wrong) day, undermining the >5000/day CE-limit mitigation the split exists to provide.
- FIX: Give external ExtractedIssues a unique synthetic key (e.g. set Key to a generated id like engineId+ruleId+component+line in extIssuesToExtracted), or key the override map on something guaranteed unique per finding (component+line+index) rather than the SonarQube issue key.
- NOTE: The collision is real and confirmed by reading the actual code paths.

CONFIRMED MECHANISM:
1. extIssuesToExtracted (internal/migrate/tasks_projectdata.go:1175-1186) builds ExtractedIssue without ever setting Key, so every external issue has Key=="". (toExtractedIssues at :1161 DOES set Key for nati

### [LOW] Override sentinel uses dateMs==0 to mean 'no override', conflicting with a legitimate epoch-0 date
- unit: scanreport | file: internal/scanreport/backdate.go:197-200 | cat: logic-error
- DESCRIPTION: applyIssueDates reads dateMs := overrides[iss.Key] and treats dateMs==0 as 'no override present', falling back to the issue's real creation date. Because overrides is a map[string]int64, a missing key also yields 0 — so the sentinel and a genuinely-stored value of 0 are indistinguishable. A synthetic override date computed as g.dateMs - N*oneDayMs in buildSafetySplitOverrides can land on exactly 0 (epoch 1970-01-01 midnight UTC) when the day group is N days after epoch, and any issue whose stored override is exactly 0 would be silently treated as 'no override' and fall back to its raw creation date instead.
- WHY: An issue legitimately assigned a synthetic override of 0ms (or, in the degenerate case, a real creation date of exactly epoch 0) is treated as having no override, so the safety-split date is ignored. This is a narrow edge case (requires the synthetic date to compute to exactly 0), hence low severity, but it is a genuine sentinel/zero-value confusion.
- FIX: Use the comma-ok form to distinguish presence from value: `if d, ok := overrides[iss.Key]; ok { dateMs = d } else { dateMs = issueDateMs(iss, fallbackMs) }`.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] CE poll response body not closed when status check leads to continue after a read of a >=400 response
- unit: scanreport | file: internal/scanreport/submit.go:189-200 | cat: resource-leak
- DESCRIPTION: In PollCETask the happy path reads and closes the body correctly (io.ReadAll then resp.Body.Close()). This is actually handled. Re-examined: body is read and resp.Body.Close() is called on line 195 before the status-code branch, so there is no leak. No action needed — included only to document that the loop's body handling was verified clean.
- WHY: On re-verification the body is always closed at line 195 before any continue, so there is no leak. This entry is informational and can be disregarded; not a real defect.
- FIX: No change required; the resp.Body.Close() at line 195 runs on every iteration before the branch that may continue.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Project/binding dedup is map-iteration-order dependent when multiple source projects collapse to one uniqueProjectKey
- unit: structure-core | file: internal/structure/projects.go:129-238 | cat: other
- DESCRIPTION: projects[uniqueProjectKey] is assigned per detail item. GenerateUniqueProjectKey returns alm+"_"+repository for ALM-bound non-monorepo projects, so two distinct Server projects bound to the same ALM repository (e.g. same repo imported twice, or main+a second project on the same repo) produce the same uniqueProjectKey. The last detailItems entry visited wins; detailItems ordering across extracts/files is not guaranteed stable, so which project's Name/GateName/Profiles/NCD end up in projects.csv is nondeterministic.
- WHY: When two Server projects share alm+repository (non-monorepo), one Project struct overwrites the other in the projects map and only one row reaches projects.csv with whichever metadata the final iteration supplied. The dropped project is not migrated, and the surviving row may carry mismatched key vs name/gate/profiles depending on iteration order. This is lower severity because same-repo non-monorepo duplicates are uncommon, but it is a real silent-drop + nondeterminism path.
- FIX: Detect collisions on uniqueProjectKey (e.g. if already present, log a warning and/or disambiguate by appending projectKey) instead of silently overwriting, and ensure the chosen winner is deterministic (e.g. by sorting detail items by projectKey before the loop).
- NOTE: low severity; reported as-is, not independently verified

### [LOW] addTemplateForAllOrgs spreads default/pattern-less templates to orgs of unrelated servers
- unit: structure-entities | file: internal/structure/templates.go:68-72 | cat: logic-error
- DESCRIPTION: For default templates or templates with empty projectKeyPattern, addTemplateForAllOrgs loops over uniqueOrgKeys (all servers) and adds the template (with item.ServerURL) to every org, with no serverURL/org affinity check.
- WHY: In a multi-server export, server A's default permission template is written as a Template row for every org including those derived from server B, so it is recreated in the wrong SonarCloud org. The pattern-matching branch (addTemplateForMatchingProjects) is correctly server-scoped via projectOrgMapping, making the default branch asymmetric. Only triggers with multiple servers in one export dir.
- FIX: Filter orgKeys to those whose server matches item.ServerURL before calling addTemplate in the all-orgs/default branch.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Composite map keys built by raw string concatenation without a delimiter can collide
- unit: structure-entities | file: internal/structure/profiles.go:97-118 | cat: logic-error
- DESCRIPTION: uniqueKey is formed as orgKey + profileKey with no separator. The same pattern is used in gates.go (orgKey + gateName, line 70/89), groups.go addGroupResult (orgKey + name, line 30), and templates.go (orgKey + t.ID, line 89). Because there is no delimiter, distinct (orgKey, key) pairs whose concatenations coincide map to the same results entry, e.g. orgKey 'org1' + 'AGate' collides with orgKey 'org1A' + 'Gate'.
- WHY: orgKeys are host/path strings (GenerateUniqueBindingKey can produce values like 'github.com/acme') and gate/group/template names are arbitrary user-controlled strings, so a boundary ambiguity can cause one entity's row to silently overwrite another's in the results map, dropping a gate/profile/group/template from the migration. Low likelihood in typical data but a real correctness hazard with no guard.
- FIX: Use a delimiter that cannot appear in either component (e.g. orgKey + "\x00" + key) or a struct key {Org, Key} for these maps.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] addProfileGroups matches profile groups without server scoping, unlike the template path
- unit: structure-entities | file: internal/structure/groups.go:113-137 | cat: logic-error
- DESCRIPTION: addProfileGroups builds profileOrgs keyed only by p.SourceProfileKey (no ServerURL) and looks up profileOrgs[profileKey] using the extract item's profileKey without item.ServerURL. By contrast addTemplateGroups correctly keys templateOrgs by t.ServerURL+t.SourceTemplateKey and looks up item.ServerURL+templateID.
- WHY: If two source servers ever share a profile key, group assignments from one server's profile would be applied to the other server's org(s). SonarQube profile keys are server-local UUIDs so collision is unlikely in practice, but the asymmetry with the template path means this path loses server isolation that the rest of the module enforces.
- FIX: Key profileOrgs by p.ServerURL+p.SourceProfileKey and look up with item.ServerURL+profileKey, matching addTemplateGroups.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] latestID returns "" if any extract dir name sorts <= empty string, but real risk is non-lexicographic IDs
- unit: structure-entities | file: internal/structure/extract.go:62-71 | cat: logic-error
- DESCRIPTION: latestID picks the lexicographically greatest directory name as the 'latest' extract. This is correct only while IDs use the zero-padded 'YYYY-MM-DD-NN' format (NN is %02d, so up to 99 runs/day sort chronologically).
- WHY: If a run dir ever exceeds 99 runs in a day (NN becomes 3 digits) or a legacy/unpadded ID is present, lexicographic order diverges from chronological order and ReadExtractData would silently read an older extract for that server URL, migrating stale data. Bounded by the current ID generator (%02d), so this is a latent constraint rather than an active bug today.
- FIX: Either keep NN zero-padded to a fixed width that exceeds expected runs, or parse the date+counter and compare numerically rather than relying on string ordering.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] collectExtractSkipped under-reports unused profiles/gates across multiple servers (dedup key includes serverURL, mapped-key lookup does not)
- unit: summary-collect | file: internal/report/summary/collect.go:660-684 | cat: logic-error
- DESCRIPTION: The per-item dedup key is extractKeyFor(def, name, language, serverURL) which is server-scoped (serverURL|name[|language]), but the 'is this entity used' check looks up mappedKeyFor(def, name, language) in mappedKeys, which is NOT server-scoped (name or name|language). buildMappedKeys likewise stores only the server-agnostic key. So if the same (name, language) entity exists on server A (referenced by a migrated project) and server B (referenced by nothing), server B's record is treated as 'used' and is silently dropped from the Skipped/unused bucket.
- WHY: In a multi-SonarQube-Server extract where two servers each have a profile/gate with the same name+language, an unused profile on one server is incorrectly classified as used and never surfaced as 'Not used by any migrated project', so the operator's report omits a legitimately-skipped entity. The seen-dedup is server-scoped (so both records are processed), but the mapped-key lookup collapses servers, producing the wrong skipped/unused determination.
- FIX: Make the used-set check server-scoped too: have buildMappedKeys store keys that include item.ServerURL (and the InputTask records must carry a server identifier) and compare with a server-scoped key in collectExtractSkipped, OR confirm that names are globally unique across servers and remove the serverURL from extractKeyFor for consistency. As written the two key spaces are mismatched.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] truncate() byte-slices multibyte UTF-8, corrupting non-ASCII Organization names in the PDF
- unit: summary-render | file: internal/report/summary/pdf.go:1459-1465 | cat: off-by-one
- DESCRIPTION: truncate() compares and slices on byte indices: `if len(s) <= maxLen` then `return s[:maxLen-3] + "..."`. `len(s)` is the byte length and `s[:maxLen-3]` is a byte slice. When `s` contains multibyte UTF-8 runes (accented Latin, Cyrillic, Greek, CJK — all valid SonarQube org names that survive sanitizeForPDF, which only strips astral-plane runes >= U+10000), the cut at byte offset `maxLen-3` can fall in the middle of a multibyte rune, producing an invalid UTF-8 byte sequence that fpdf renders as a mojibake/replacement glyph at the truncation point.
- WHY: In renderUnifiedTable the live caller is `truncate(row.org, 24)` (pdf.go:682) for the Organization column of every non-predictive, non-Portfolio section. Any organization name longer than 24 bytes whose 22nd–24th byte region lands inside a 2–4 byte rune (e.g. a Cyrillic or accented-Latin org name) gets sliced mid-rune, so the last rendered character in the cell is garbage. The accompanying TestTruncate only exercises ASCII ("short", "this is a long string"), so the corruption is untested. (The other call site at pdf.go:678 is dead because wrapName is hardcoded true at line 591.)
- FIX: Operate on runes, not bytes: convert to `[]rune` (or use utf8.RuneCountInString for the length check and accumulate runes up to maxLen-3) before slicing, e.g. `r := []rune(s); if len(r) <= maxLen { return s }; return string(r[:maxLen-3]) + "..."`. This keeps the cut on a rune boundary and makes the length comparison count characters rather than bytes.
- NOTE: The byte-slicing mechanism is real and I proved it empirically: truncate() at pdf.go:1459-1465 uses len(s) (byte length) and s[:maxLen-3] (byte slice). Running it on a Cyrillic string ("организация-длинное-название", 54 bytes) with maxLen=24 produced invalid UTF-8: "организаци\xd1..." — a dangling l

### [LOW] applyGateMappingNotes rebuilds EntityItem and silently drops the original item.Issues
- unit: summary-failures | file: internal/report/summary/gate_notes.go:189-201 | cat: data-loss
- DESCRIPTION: When a Succeeded quality-gate row matches a mapping note, the code constructs a brand-new EntityItem copying Name/Language/Organization/Detail and setting Issues to a fresh copy of note.Issues only. It does NOT carry over item.Issues. Any Issues lines previously attached to that Succeeded entity are discarded when it is moved into NearPerfect/Partial.
- WHY: In the current pipeline, collectSucceeded (collect.go:583) never sets Issues, and applyGateMappingNotes runs on a freshly-collected Succeeded list, so item.Issues is nil at this point and nothing is lost today. It becomes a real data-loss bug the moment any earlier step attaches Issues to a Succeeded gate before this function runs (e.g. if the apply-step ordering in collect.go changes, or a future per-gate annotator is inserted earlier). The contrast with the project/template paths (project_failures.go:282 and global_failures.go:68 both do `append(append([]string(nil), item.Issues...), ...)` to preserve prior Issues) shows the intended pattern was to preserve existing Issues.
- FIX: Either mutate in place like applyPortfolioClassifications (item.Issues = append(item.Issues, note.Issues...)) or, when rebuilding, seed Issues with the existing ones: Issues: append(append([]string(nil), item.Issues...), note.Issues...).
- NOTE: low severity; reported as-is, not independently verified

### [LOW] applyProfileFindings rebuilds EntityItem and silently drops the original item.Issues
- unit: summary-failures | file: internal/report/summary/profile_notes.go:215-221 | cat: data-loss
- DESCRIPTION: Identical pattern to applyGateMappingNotes: a matched Succeeded quality-profile row is reconstructed into a new EntityItem whose Issues is set solely to a copy of f.Issues, discarding any pre-existing item.Issues before routing it into NearPerfect/Partial.
- WHY: Not triggered today because collectSucceeded does not populate Issues and this runs first on the fresh Succeeded slice (item.Issues is nil). But it is a latent data-loss hazard: if any annotator attaches Issues to a Succeeded profile before applyProfileFindings, those lines vanish. The project-level and template-level routers in this same package deliberately do `append(append([]string(nil), item.Issues...), ...)` to preserve prior Issues, so the divergence here is inconsistent and fragile.
- FIX: Preserve existing Issues when moving: Issues: append(append([]string(nil), item.Issues...), f.Issues...), or mutate item.Issues in place as applyPortfolioClassifications does.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Quality-gate failure detection compares event name to all-caps "FAILED", which SonarQube never emits
- unit: report-common-a | file: internal/report/common/pipelines.go:57-76 | cat: api-misuse
- DESCRIPTION: checkFailedEvents iterates analysis events and treats a scan as failed only when category == "QUALITY_GATE" && name == "FAILED". The events come straight from SonarQube's api/project_analyses/search response (no normalization in the extractor — EnrichRaw only adds metadata keys, it does not touch event names). SonarQube QUALITY_GATE event 'name' values are not the all-caps token "FAILED"; they are human-readable status strings such as "Failed"/"Passed" (or, on status changes, forms like "Red (was Green)"). An exact case-sensitive equality against "FAILED" will not match those, so the QUALITY_GATE branch is effectively dead.
- WHY: failed_scans and failed_scans_30_days will always remain 0 even when projects have failing quality-gate analyses, so any report column or downstream consumer relying on failed-scan counts under-reports failures (silently shows zero failures). This only surfaces once the projectKey enrichment bug above is fixed and scans are actually processed.
- FIX: Match the actual SonarQube event semantics: compare case-insensitively and against the real status wording (e.g. strings.EqualFold(name, "Failed") or detect a "Red" status / status-change name), ideally driven by an inspection of a real api/project_analyses/search payload rather than a guessed literal.
- NOTE: REAL bug, empirically confirmed against on-disk extracted data. checkFailedEvents (internal/report/common/pipelines.go:70) treats a scan as failed only when category=="QUALITY_GATE" && name=="FAILED". ExtractString (internal/report/jsonpath.go:108-114) does zero case normalization (returns raw via f

### [LOW] time.Time scan dates are placed into report rows but FormatValue has no time.Time case, producing malformed date cells
- unit: report-common-a | file: internal/report/common/pipelines.go:141-166 | cat: logic-error
- DESCRIPTION: buildPipelineOverview (line 143) and buildScanDetails (lines 157-158) store raw time.Time values (first_scan/last_scan) directly into the row maps that are handed to report.GenerateSection. report.FormatValue (internal/report/markdown.go:165-188) has cases for bool/int/float64/[]string/[]any/string and a default fmt.Sprintf("%v", v); there is no time.Time case, so a time.Time falls through to %v and renders as e.g. '2026-04-15 10:00:00 +0000 +0000' (doubled zone, possibly monotonic clock noise) instead of a clean date. The sibling code in projects.go:212 deliberately calls scanDate.Format("2006-01-02") before storing, showing the intended presentation.
- WHY: The 'First Run' / 'Most Recent Run' / 'First Scan' / 'Most Recent Scan' columns in the CI Environment Overview and Project Scan Details tables render verbose/ugly Go time strings rather than dates, an incorrect (not merely unstyled) rendering of the underlying value. Latent today because the projectKey bug keeps these sections empty, but wrong once scans flow through.
- FIX: Format the times before inserting them into the row maps (date.Format("2006-01-02") like projects.go), or add a case time.Time in FormatValue that formats with a canonical layout.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] assignDefaultTemplates overwrites a pattern-matched project_count for templates that are also the Projects default
- unit: report-common-a | file: internal/report/common/permissions.go:118-126 | cat: logic-error
- DESCRIPTION: matchPatternTemplates sets tmpl["project_count"] = len(matched) for templates with a regex pattern (line 114) and removes matched keys from remaining. assignDefaultTemplates then unconditionally overwrites tmpl["project_count"] = len(remaining[sid]) for any template whose defaults contains "Projects" (line 122-123). If a single template both has a project-key pattern and is the default template for the Projects (TRK) qualifier, its accurate pattern-match count is clobbered and replaced with the count of leftover (unmatched) projects — which excludes the very projects the pattern matched.
- WHY: For a server where the default Projects template also defines a projectKeyPattern, the Projects column for that template reports the wrong number (unmatched remainder instead of pattern matches, or instead of all projects). It also assumes at most one Projects-default template; if two templates were both flagged Projects-default each would independently be assigned the full remaining count. This is an edge case (SonarQube normally has one default per qualifier and default templates usually lack patterns) hence low severity.
- FIX: Only assign the remaining-projects count to a default template when it has no pattern (or add its remaining count to the existing pattern count rather than overwriting), and guard against assigning the same remaining set to multiple templates.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] formatFloatCommas mangles negative fractional values by stripping the sign instead of the dot
- unit: report-common-b | file: internal/report/markdown.go:195-201 | cat: logic-error
- DESCRIPTION: formatFloatCommas computes intPart = int(f) (truncates toward zero) and frac = Sprintf("%.2f", f-intPart), then takes frac[1:] expecting to strip a leading '0' before the dot. For a value with magnitude < 1 and negative, e.g. f=-0.57: intPart=0, f-intPart=-0.57, frac="-0.57", frac[1:]="0.57" (the minus is stripped, not the dot), and the result becomes addCommas("0")+"0.57" = "00.57" — wrong value, wrong sign. For a value like f=-12.50: intPart=-12, frac of -0.50 = "-0.50", frac[1:]="0.50", result "-12"+"0.50" = "-120.50".
- WHY: Task queue times are computed as startedTime.Sub(submittedTime).Seconds()*1000 (tasks.go:62). If a task's startedAt precedes its submittedAt (clock skew or out-of-order timestamps), queueMs is negative, and min_queue_time / avg_queue_time become negative floats that flow into FormatValue -> formatFloatCommas, rendering corrupted numbers (e.g. '-120.50' or '00.57') in the Tasks table.
- FIX: Handle the sign explicitly: take neg := f < 0; work with math.Abs(f); compute intPart and fractional part on the absolute value; build the decimal from the absolute fractional string; prepend '-' if neg. Or format the whole float once and insert commas into the integer portion only.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Token entry created for servers that have zero qualifying user tokens
- unit: report-common-b | file: internal/report/common/tokens.go:19-28 | cat: logic-error
- DESCRIPTION: ProcessTokens calls ensureTokenEntry(tokens, sid) (line 19) for every getUserTokens record BEFORE the filters that skip SonarLint tokens, non-USER_TOKEN types, and empty name/type (lines 22-27). A server whose only token records are all filtered out still gets a tokens[sid] entry with all-zero counts, which flattens into a Tokens table row showing 0/0/0 for that server.
- WHY: GenerateTokenMarkdown emits one row per tokens[sid] entry. A server that has only SonarLint or bot/global-analysis tokens (all filtered) will appear as a spurious all-zeros row instead of being omitted, polluting the assessment output. ensureTokenEntry should run only after a record passes the filters.
- FIX: Move the ensureTokenEntry call to after the name/type and SonarLint/USER_TOKEN filter checks, immediately before updateTokenStats, so entries are created only for servers with at least one qualifying token.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Unique user count collapses distinct users that share a blank external identity into login fallback inconsistently
- unit: report-common-b | file: internal/report/common/users.go:85-97 | cat: logic-error
- DESCRIPTION: countUniqueUsers uses external_id as the dedup key, falling back to login only when external_id is empty. SonarQube users with the same externalIdentity value (e.g. multiple users with empty-string-normalized-to-non-empty shared external IDs, or imported users sharing an identity) would be merged, while users with empty external_id are deduped by login. The mixing of two key namespaces (external identity vs login) in one set means a user whose external_id happens to equal another user's login would be incorrectly merged.
- WHY: external_id (externalIdentity) and login are different namespaces; combining them into a single dedup set 'seen' can both over-count (two namespaces never collide so legitimately-same users counted twice when one has external_id and one falls back to login) and under-count (a coincidental external_id==login collision merges two distinct users). The Unique Users metric in the report can be off.
- FIX: Dedup on a stable single identity. Prefer login as the canonical unique key (logins are unique per SonarQube instance), or namespace the fallback (e.g. 'ext:'+id vs 'login:'+login) so the two key spaces cannot collide.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] assignTier returns 'xs' (LOC>=0) for projects with no usage data instead of 'unknown'
- unit: report-common-b | file: internal/report/common/projects.go:122-129 | cat: logic-error
- DESCRIPTION: ProcessProjectDetails initializes loc=0 and tier="unknown". processProjectUsage only overwrites tier for projects that appear in getUsage. assignTier(loc) returns the first tier whose minLOC <= loc; the last tier is {"xs", 0}. So any project that DOES appear in getUsage but reports linesOfCode=0 (or is missing the field, defaulting to 0) is assigned tier 'xs' rather than 'unknown'. Only projects entirely absent from getUsage keep 'unknown'.
- WHY: A project present in usage data with 0 LOC (never scanned, or empty) is reported as tier 'xs' (a real size bucket) rather than 'unknown', conflating 'tiny project' with 'no measurable code'. This skews tier distribution counts in the assessment. The {"xs",0} threshold combined with the loc default of 0 makes the unknown/xs boundary ambiguous.
- FIX: Treat loc<=0 as 'unknown' explicitly: in assignTier, return 'unknown' when loc<=0 (and set the xs threshold to >=1), or only call assignTier when the usage record actually contained a linesOfCode value greater than zero.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Nested edition fallback reads System.Edition, but api/system/info exposes edition at Statistics.edition
- unit: common | file: internal/common/edition.go:38-46 | cat: api-misuse
- DESCRIPTION: When the top-level "edition" key is absent, ParseEdition tries the nested path info["System"]["Edition"]. Per SonarSource documentation/community reports, the edition value in `api/system/info` lived under the `Statistics` object (`Statistics.edition`), not `System.Edition`; the report-side code at internal/report/common/servers.go:26 also reads "System.Edition", suggesting the path was assumed rather than verified. As a result the nested fallback never resolves on real responses.
- WHY: If the top-level "edition" key is ever missing from the response (older SonarQube, or a payload shape change), the intended nested fallback resolves nothing and ParseEdition returns EditionCommunity, downgrading the detected edition and stripping edition-gated tasks. Because the top-level key normally exists, this is latent, but the fallback it is meant to provide is effectively dead.
- FIX: Probe the documented locations: top-level "edition", then "Statistics" -> "edition", then "System" -> "Edition". Verify against a live api/system/info response for the target SonarQube versions and align report/common/servers.go to the same path.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] GetPaginated returns only page 1 when paging.total is absent (total==0)
- unit: common | file: internal/common/rawclient.go:165-190 | cat: pagination
- DESCRIPTION: GetPaginated computes pages := TotalPages(total, MaxPageSize) where total comes from ExtractTotal(body, opts.TotalKey) defaulting to "paging.total". If the endpoint omits that key (different total field, or a response with items but no paging block), ExtractTotal returns 0, TotalPages(0, ...) returns 0, and the page-2..N loop never executes — so any items beyond the first page are silently dropped with no error. The number of items already returned on page 1 (which may equal MaxPageSize) is not cross-checked against total.
- WHY: All current callers hit endpoints that return paging.total, so this is not triggered today. But it is a real foot-gun: a future caller pointing GetPaginated at an endpoint with a differently-named total (or no total) would silently receive a truncated first page and the migration would under-count/under-migrate with no failure signal. Relying solely on a possibly-absent total to bound pagination is fragile.
- FIX: When total resolves to 0 but the first page returned a full page (len(items) >= MaxPageSize), keep fetching subsequent pages until a short/empty page is returned, rather than stopping at page 1. Alternatively, log a warning when total is 0 yet a full page was returned so silent truncation is visible.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] createGates dedup by name only collapses same-named gates across servers, breaking the (server_url, gateName) join for gate-condition notes
- unit: predict | file: internal/predict/synthesize.go:299-308 | cat: logic-error
- DESCRIPTION: writeCreateJSONL dedupes synthetic create rows by entity identity (dedupKey = name, or language/name for profiles) at lines 303-309, keeping the FIRST row encountered and discarding later ones. For gates the dedupKey is just `name` (gates carry no language field). The surviving row keeps the first occurrence's server_url. synthesizeAddGateConditionsNotes (gate_notes.go:88) then joins conditions to gates via condKey{serverURL, gateName} using the createGates row's server_url. If the same gate name exists on two different source servers, only one server_url survives in createGates, so conditions extracted from the other server (keyed by their own ServerURL) never match and their remap/drop notes are silently lost from the predictive report.
- WHY: In a multi-server migration where two SonarQube Server instances both have a custom quality gate named e.g. "Backend QG", the dedup keeps one (server_url=A). Gate conditions extracted from server B (gate_notes.go byGate keyed on serverURL=B) will not be joined to any createGates row, so any dropped/remapped condition notes for server B's gate are omitted, under-reporting NearPerfect/Partial classification for that gate. The real migrate keys conditions per (gateName, serverURL) against per-server createGates rows and does not collapse across servers, so it does not have this blind spot.
- FIX: Include server_url in the gate dedupKey (e.g. dedupKey = server_url + "\x00" + name) for org-scoped sections, or dedup gates by (sonarcloud_org + name) consistent with how conditions are later joined; alternatively join gate-condition notes by gate name across all servers rather than by (serverURL, gateName). At minimum, do not silently drop conditions whose server_url has no surviving createGates row.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] checkGroupCount and checkGroupMembership silently truncate at 100 groups (hardcoded ps=100, no pagination)
- unit: regtest | file: internal/regtest/checks.go:494-544 | cat: pagination
- DESCRIPTION: checkGroupCount uses queryCount on api/user_groups/search with ps=100 for both SQS and SC, so it counts at most 100 groups per side. checkGroupMembership likewise fetches the SQS group list with a single ps=100 page and iterates only those groups. Neither paginates; api/user_groups/search is a paginated endpoint and an instance can have more than 100 groups.
- WHY: An SQS instance with >100 groups: checkGroupCount compares min(realSQS,100) vs min(realSC,100). If SQS has 150 groups and SC has 150, both report 100 and the check PASSES while hiding that 50 groups were never counted; if the truncation points differ it can also FAIL spuriously. checkGroupMembership never even examines groups beyond the first 100, so membership regressions on those groups are invisible. The check appears to validate group migration but provides false assurance once the group set exceeds one page.
- FIX: Paginate both sides using s.sqsRaw.GetPaginated / s.scRaw.GetPaginated with ResultKey 'groups' and TotalKey 'paging.total' (the same pattern getProjects uses), and count/iterate the full result set rather than a single 100-row page.
- NOTE: CONFIRMED REAL but over-rated; correct severity is low. Mechanism verified by reading the code:

1. queryCount (internal/regtest/helpers.go:40-50) fetches ONE page via raw.Get and returns len(ExtractArray(...)) — it never reads paging.total and never loops pages. With ps=100 it returns at most 100.


### [LOW] Issue resolution distribution checks use the legacy 'resolutions' filter, which does not capture modern ACCEPTED/FALSE_POSITIVE issue states the migrator actually syncs
- unit: regtest | file: internal/regtest/checks.go:37-39, 139-164 | cat: api-misuse
- DESCRIPTION: checkIssueDistribution is registered for resolutions FALSE-POSITIVE, WONTFIX and FIXED, sending resolutions=<value> to api/issues/search on both SQS and SC. The migration tool itself (internal/migrate/tasks_issuesync.go:621) queries SonarCloud with the modern unified enum issueStatuses=OPEN,CONFIRMED,FALSE_POSITIVE,ACCEPTED, and its triage-state logic (lines 258-264) treats issueStatus=ACCEPTED / FALSE_POSITIVE as the authoritative signal, with resolution=WONTFIX/FALSE-POSITIVE only as a legacy fallback. On post-10.4 SonarQube/SonarCloud an accepted issue is reported as status=RESOLVED, resolution=WONTFIX, issueStatus=ACCEPTED.
- WHY: The regtest's 'WONTFIX resolution' check is intended to verify accepted-issue parity, but accepted issues are now surfaced primarily via issueStatus=ACCEPTED. If SonarCloud's resolutions facet does not populate resolution=WONTFIX identically to SQS for migrated ACCEPTED issues (the unified-status migration path sets issueStatus, not necessarily the legacy resolution facet), the SQS vs SC counts diverge and the check reports a FAIL even though the triage state migrated correctly per the tool's own model. The comparison is keyed to a deprecated facet that no longer reflects how acceptance is represented or migrated.
- FIX: Add (or switch to) issueStatuses-based distribution checks (ACCEPTED, FALSE_POSITIVE) mirroring the filter the migrator uses, and treat the legacy resolutions checks as informational only. At minimum verify empirically that resolutions=WONTFIX returns the same population on both endpoints before treating a mismatch as a failure.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] PromptChoice silently returns index 0 when the response is an unmatched string or unexpected type
- unit: gui | file: internal/gui/web_prompter.go:118-139 | cat: logic-error
- DESCRIPTION: PromptChoice handles resp.Value as float64 (returns int(v)) or string (matches against options). If the value is a string that matches none of the options, or is any other type (nil, bool, etc.), the switch falls through to `return 0, nil`, silently selecting the first option with no error.
- WHY: A protocol mismatch or a stray/garbled browser response that does not match any option will cause the wizard to silently proceed as if the user picked option index 0, instead of failing or re-prompting. This can drive the migration down the wrong branch (e.g. picking the first organization/mapping choice) without any indication of error. The float64 path is the normal case, so this only triggers on malformed/unexpected client input, hence low severity.
- FIX: Return a non-nil error for unmatched strings and unexpected types (e.g. `return 0, fmt.Errorf("invalid choice response: %v", resp.Value)`) rather than defaulting to 0, so the caller can surface or retry instead of silently choosing the first option.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] Hub.Send releases the mutex between reading conn and writing, allowing a write to a connection already replaced/closed by ServeWS
- unit: gui | file: internal/gui/websocket.go:43-65 | cat: concurrency
- DESCRIPTION: Send locks mu, reads conn into a local, unlocks, then re-locks mu to call conn.WriteMessage. Between the unlock and the re-lock, ServeWS can run (a new WebSocket connecting replaces h.conn and Closes the old one). Send then writes to the now-closed old connection.
- WHY: When a second browser tab/connection arrives mid-prompt (ServeWS closes the prior h.conn at line 78), an in-flight Send captured the old conn and will WriteMessage to a closed socket. The write returns an error that is only logged ('gui: write error'), and the message intended for the user is silently dropped. Because the read of conn and the write are not done under a single critical section, the freshness check is racy. Consequence is a dropped/lost server message rather than a crash, so severity is low; this is a single-user local tool where concurrent connections are rare.
- FIX: Hold the mutex across both the conn read and the WriteMessage (single critical section), or re-check `h.conn == conn` after re-acquiring the lock before writing. Holding the lock for the duration of the write is acceptable here since gorilla/websocket already requires serialized writes.
- NOTE: low severity; reported as-is, not independently verified

### [LOW] debug:true in a unified/sectioned transfer config file is silently dropped
- unit: cmd-core | file: cmd/transfer.go:272 (root cause: internal/migrate/config_file.go:146-178) | cat: data-loss
- DESCRIPTION: loadTransferFileDefaults sets cfg.debug = migrateCfg.Debug (transfer.go:272). For the unified source/target config shape -- which transfer's own help text documents as THE config shape (transfer.go:119-135) -- migrate.toMigrateConfig never assigns cfg.Debug (internal/migrate/config_file.go:146-178; Debug is only set in the flat/default case at line 211). The side-sectioned and command-sectioned branches also omit it. So migrateCfg.Debug is always false for those shapes, transfer's cfg.debug is false, and both the extract and migrate phases run at INFO level even though the config requested debug.
- WHY: An operator writes the documented unified config { "debug": true, "source": {...}, "target": {...} } and runs `transfer -c config.json` to capture verbose API payloads while diagnosing a failed migration. Debug logging never turns on; the only working path is the --debug CLI flag (correctly picked up via the persistent root flag at transfer.go:319, verified by a cobra inheritance test). The config-file setting is honored for the flat shape but silently ignored for the shape the transfer docs tell users to use.
- FIX: In internal/migrate/config_file.go toMigrateConfig, propagate Debug in every branch: for the unified shape set cfg.Debug = s.Debug after reading the Target block, and add the same assignment in the SonarCloud and Migrate branches. Mirror in the extract loader if it has the same gap. Alternatively have transfer OR-in the extract config's debug value.
- NOTE: low severity; reported as-is, not independently verified

